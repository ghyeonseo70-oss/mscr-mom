# 프로젝트 현황 (인수인계, 2026-08-27 정리)

MSCR-MOM(자기 연성 카테터 로봇) 논문(Park et al., IEEE RA-L 2024) 재현 + 홀센서로 접촉(충돌)
감지 기능 확장 연구. 울산대 김현서 학생, 전기전자공학전공.
**새 세션에서 할 말**: "PROJECT_STATUS.md 읽고 여기서부터 이어서 진행해줘"

---

## 🎯 지금 할 일 (우선순위 순)

**🔧 2026-09-19(31번) 30번의 "s 악화" 결론 정정 - 측정 착시였고, 23번 스윕은 실제로
목표를 달성했음.** 30번(다른 컴퓨터)이 데이터 518→655행으로 늘린 뒤 "s R² 0.910→0.844,
s>=60mm MAE 9.49→11.06mm 악화"로 기록했는데, **홀드아웃 구성이 바뀐 탓**이었음(5번에서
이미 겪은 것과 같은 착시). 23번 스윕이 s=60-100mm 구간만 집중적으로 채웠기 때문에 그 20%가
홀드아웃에 들어가면서 **신규 홀드아웃 26행 중 73%가 s>=60mm**(기존 99행은 34%)로 난이도
구성 자체가 바뀜. `_diag_holdout_growth_effect.py`(신규)로 **기존 99행만 떼어내 사과 대
사과로 재비교**:

| 지표 | 26번(518개 학습) | **31번 재측정(655개 학습, 동일 99행)** |
|---|---|---|
| balanced acc | 83.0% | **87.0%** ↑ |
| s R²/MAE | 0.910 / 5.77mm | **0.919 / 5.34mm** ↑ |
| **s>=60mm MAE** | 9.49mm | **8.93mm** ↑ (23번 목표 달성) |
| Fx_board R²/MAE | 0.626 / - | **0.728 / 0.0047mN** ↑ |
| Fy_board R² | 0.855 | 0.767 ↓ (시드 노이즈 범위 가능성, 25번 참고) |

**Fy_board 빼고 전부 개선됨. 30번의 "악화" 결론은 폐기하고 이 표를 기준으로 볼 것.**

**+ 28번(R^2 함정)의 교과서적 사례 추가 확보**: 신규 26행만 따로 보면 Fx R²=-6.4,
**Fy R²=-92.7**이라는 극단적 수치가 나오는데, **정작 MAE는 Fx 0.0020mN / Fy 0.0084mN으로
기존 행(0.0047 / 0.0091)보다 오히려 좋음**. 원인은 팁근처 접촉이라 정답 힘 값이 전부
고만고만하게 작아서 **정답값 표준편차가 기존 행의 1/9(Fx), 1/13(Fy)** 로 붕괴 → R² 분모가
무너진 것. **앞으로 힘 관련 수치는 R^2 단독으로 절대 판단하지 말 것**(28번 결론 재확인).
단 **s는 신규 행에서 MAE 12.50mm vs 5.34mm로 진짜 나쁨**(절대지표라 착시 아님) - 팁근처
위치추정이 원래 어렵다는 기존 사실과 일치.

**🔜 2026-09-19(30-b번) push_depth 축 FEA 확보 착수 - 형상 추정 전환용.** 목표를
"힘 정량화"에서 **"충돌 시 로봇이 어떤 형상이 되는가"**로 트는 방향을 검토 중인데(아래
배경 참고), 기존 데이터가 **전부 push_depth=0.10mm 고정**이라 "충돌 강도에 따라 얼마나 더
휘는지"를 학습할 데이터가 아예 없음. 원래 스윕 주석이 "힘/깊이는 덜 중요하다는 전제"라고
명시하고 있어서 설계 단계부터 의도적으로 제외된 축이었음(당시 목표가 "접촉 위치 감지"라
합리적 선택이었음). `sweep_push_depth_matv2.py`(신규): **이미 0.10mm에서 수렴 성공한
조합만** 골라(수렴실패 낭비 최소화) L_M/|phi|고저/s구간 층화추출 후 다른 깊이로 재실행 -
같은 (L_M,phi,s)에 깊이만 다른 짝지어진 데이터를 만들어 깊이 효과를 분리해 학습 가능하게
함. 이 컴퓨터에서 0.05/0.20mm x 30조합 = 60케이스 실행 중(~4.5시간).
**끝나면 할 일**: (a) 깊이-변형 관계가 선형인지 확인, (b) 학습 스크립트의 `worker()`가
`FIXED_DEPTH=0.10`으로 깊이를 고정 샘플링하고 있으므로 **깊이도 랜덤 샘플링하도록 수정**해야
합성데이터에 이 축이 반영됨, (c) `shape_head`(tip_ux/uy/theta 직접 예측) 추가 검토.

**✅ 2026-09-14(29번) 기하학 재구성 재검증 - 확실한 성공, 교수님 지적 완전히
검증됨.** 18번(2026-08-27)에서 "예측 phi에 오차가 있어 시기상조"였던
`_diag_force_geom_reconstruction.py`(F_mag*cosθ/sinθ 재구성)를 25번의 known-input
구조에 맞게 갱신 후 재실행(체크포인트: 25/26번 최종본, 재학습 없이 바로 검증 가능했음).
θ 계산에 쓰는 L_M/phi가 이제 "예측값"이 아니라 real_holdout_rows의 **진짜 값**이라
예측오차가 낄 자리가 없어짐(s만 여전히 s_head 예측, R²=0.910):

| 구간 | 직접회귀 R²/MAE | **재구성 R²/MAE** |
|---|---|---|
| 전체(n=99) | 0.626 / 0.0049mN | **0.743 / 0.0040mN** |
| |phi|<90(n=45) | 0.720 / 0.0047mN | **0.827 / 0.0033mN** |
| |phi|>=90(n=54) | 0.440 / 0.0050mN | **0.574 / 0.0047mN** |

**모든 구간에서 R^2와 MAE가 동시에 개선됨**(28번 교훈 반영해 MAE도 같이 확인, R^2 함정
아님을 확인). 전체 R^2=0.743은 예전 오라클(L_M/phi/s 셋 다 진짜, R^2=0.749)에 거의
근접 - s가 예측값(R^2=0.910)인데도 오라클급이 나옴. **교수님이 처음 지적한 "Fy는
F_mag*sinθ로 당연히 나와야 하는 값"이라는 통찰이 완전히 검증됨.**

수정 내역: (1) `SingleProbeClassifier`를 known-input 구조(forward(x,config))로 갱신,
(2) **부수적으로 발견한 버그 수정**: 이 진단 스크립트가 2026-08-26 MOM 실측 변위 수정
이전 frac 근사를 계속 쓰고 있었음(train 스크립트와 다른 로직) - `mom_ux_avg_mm` 있으면
실측값을 쓰도록 맞춰서 직접회귀 기준값이 공식 수치(R^2=0.626)와 정확히 일치하게 됨.

**🔜 다음 세션**: 이 재구성을 진단(diagnostic)에만 두지 말고 **실제 추론 경로에 반영할지
결정할 것** - 예를 들어 `evaluate_real()`이나 실사용 스크립트에서 Fx_board를
`force_head` 직접출력 대신 `-F_mag_est*sinθ`(θ는 known L_M/phi + 예측 s로 계산)로
계산하도록 바꾸면 재학습 없이 바로 이 개선을 얻을 수 있음. cos(theta)~0(거의 90도) 근처만
직접예측값으로 폴백하면 안정적(이번 검증에서 99/99 전부 안정적으로 재구성됨).

**⚠️ 2026-09-14(28번) "|phi|>=90이 특별히 약하다"는 그동안의 판단, R^2 통계적
함정이었을 가능성 큼 - 앞으로 R^2 대신 MAE로 판단할 것.** `plot_final_scatter_summary.py`
산점도(known input 반영판)를 보다가 |phi|<90/|phi|>=90 산점이 육안으로 비슷해 보여서
직접 MAE까지 쪼개봄(체크포인트: 25/26번 최종본, 시드43+파인튜닝없음+가중치0.1):

| 구간 | n | R² | **MAE** | 실제값 표준편차 |
|---|---|---|---|---|
| |phi|<90 | 45 | 0.720 | 0.0047mN | 0.0216mN |
| |phi|>=90 | 54 | 0.440 | **0.0050mN** | 0.0140mN |

**절대오차(MAE)는 두 구간이 사실상 동일**(0.0047 vs 0.0050mN)한데 R^2는 0.720 vs 0.440으로
크게 벌어짐 - R^2=1-잔차/분산이라, **|phi|>=90 구간은 정답 값 자체의 분산이 작아서
(표준편차 0.014 vs 0.022mN) 똑같은 절대오차도 R^2가 기계적으로 더 낮게 나오는 구조적
함정**임. 즉 지난 20~27번에 걸쳐 "|phi|>=90 Fx_board가 특별히 약한 구간"이라고 추적해온 게,
**사실은 그 구간만의 문제가 아니라 Fx_board 전체가 어디서든 비슷한 절대오차(~0.005mN)를
갖는 문제를 R^2만 보고 "고각도가 문제"라고 오판했을 가능성**이 있음.

**수정 완료**: `train_segment_classifier_singleprobe_beta0180_4seg.py`의 `evaluate_real()`이
이제 |phi|<90/|phi|>=90 둘 다 R^2와 MAE(+정답값표준편차)를 같이 출력함(다음 재학습부터
바로 확인 가능). `plot_final_scatter_summary.py`도 known input 구조로 갱신하면서 Fx_board
패널에 |phi|>=90을 X마커로 강조 표시하도록 고쳐둠.

**🔜 다음 세션**: 27번(FEA 재시도)은 여전히 유효함(데이터가 느는 건 항상 도움) - 다만
"고각도 전용 문제"라는 프레이밍은 재검토 필요. Fx_board의 진짜 문제는 **"어디서나 절대오차
~0.005mN"** 쪽에 더 가까워 보이므로, 앞으로 이 축 관련 실험/보고는 R^2 단독이 아니라
**MAE(+비교대상 구간의 정답값 표준편차)를 같이 보고 판단할 것**.

**✅ 2026-09-14(30번) 23번(저-phi 팁조밀화) 실행 + 27번 병렬화해서 이 컴퓨터에서 같이
실행 + 재학습 — 데이터는 늘렸지만, 아래 28/29번이 나중에 밝혀낸 대로 이 라운드의
"Fx_board 고각도 개선(0.440→0.461)" 판단은 R^2 단독 비교라 신뢰 불가.** 28/29번과
독립적으로(병렬 세션) 진행된 라운드라 28/29번의 MAE 병행 확인이 반영되기 전 코드로
실행됨 - 아래 수치는 **참고용으로만 볼 것**, 결론은 다음 세션 재확인 필요. 이
컴퓨터(리눅스, 64코어)에서 진행:
- **23번 실행**: `run_lowphi_tipdensify_matv2_sweep.sh`(저-phi phi=0,±30,±60 x
  s=60-100mm 오프셋격자, 160케이스) — **67개 성공(41.9%)**, 특이점 없는 구간이라
  고각도 스윕(7.1%, 아래 참고)보다 훨씬 잘 됨.
- **27번(retry_highphi_missing_matv2.py) 실행 방식 변경**: 원본은 조합을 순차 처리해서
  533개 다 돌리면 5~7일 걸리는 구조라, 같은 로직(목표격자 대비 누락 계산, STABILIZE=0.01
  명시)을 재사용하되 조합 단위로 **xargs 병렬 실행하는 래퍼
  `run_retry_highphi_missing_parallel.sh`(신규, 6-way)** 를 추가로 만들어 23번과
  동시에 돌림 - 원본 스크립트와 출력 태그(RETRY*) 호환되므로 다른 컴퓨터가 원본을
  마저 돌려도 자동으로 안 겹침. **9/14 세션 종료 시점 기준 아직 진행 중**(533개 중
  일부만 완료, 관찰된 성공률이 낮음(~8%) - STABILIZE=0.01이 효과가 크지 않을 수
  있음, 다음 세션에서 최종 결과 확인 필요).
- 그 사이 전에 다른 세션에서 만든 고각도(phi=90~150) s=60-100mm 오프셋격자 조밀화
  데이터(24개 성공, 이름은 "23번"이었지만 실제로는 사전조율 안 된 중복작업이었음)도
  버리지 않고 병합함. `fea_lm_phi_pos_matv2_all.json`: 518 → (고각도 +24) 542 →
  (저-phi 23번 +67) **609개**.
- 재학습(150k/60epoch, GPU 19.6분, 시드43, `FINETUNE_ON_REAL=0` — 26번의 채택 설정과
  동일 조건, 데이터량만 518→609로 비교): 순수 실측 홀드아웃(n=115, 해시분할이라
  기존 행 소속은 안 바뀜):
  | 지표 | 이전(518개, 26번 기준) | 이번(609개) |
  |---|---|---|
  | balanced acc | 83.0% | 85.2% |
  | s R²/MAE(전체) | 0.910/5.77mm | 0.844/6.77mm |
  | **s>=60mm(팁근처) MAE** | 9.49mm(옛 아키텍처 기준값, 22번) | **11.06mm(악화)** |
  | Fx_board(전체) | 0.626 | 0.705 |
  | **|phi|>=90 Fx_board (R²만, MAE 미기록 - 위 28번 참고)** | 0.440 | 0.461 |
  | Fy_board | 0.855 | 0.635 |

  **해석 주의**: 23번의 목표였던 s 팁근처 MAE는 데이터를 추가했는데도 오히려
  나빠짐(9.49→11.06mm) - 다만 이 9.49mm 기준값은 known-input 이전(25번 이전)
  구식 아키텍처에서 나온 수치라 완전히 동일 조건 비교는 아님(홀드아웃도 n=99→115로
  구성이 달라짐, 새로 추가된 팁근처 실측행 자체가 해시분할로 일부 홀드아웃에 들어가면서
  우연히 어려운 샘플이 몰렸을 가능성 배제 못 함). **Fx_board 고각도 "개선"(0.440→0.461)은
  위 28번 발견(R^2 함정) 이후 다시 보면 신뢰도가 낮음** - 이 라운드는 MAE 병행 출력이
  반영되기 전 코드로 돌았어서 MAE 비교가 없음, 실제로 절대오차가 개선됐는지는 다음
  재학습에서 새 `evaluate_real()`로 다시 확인해야 함. **다음 세션에서 할 일**: (1)
  27번(고각도 재시도)이 끝나면 유의미한 양의 신규 고각도 데이터가 생길 테니 그때
  현재(28/29번 반영된) 코드로 다시 재학습해서 R^2와 MAE를 함께 갱신할 것. (2) s
  팁근처 악화 원인 확인 - `_diag_s_breakdown.py`를 known-input 아키텍처에 맞게
  고쳐서(5-output 구식 SingleProbeClassifier 복제 문제, 25번 다음 세션 메모 참고)
  다시 세분화해볼 것. (3) 29번의 기하학 재구성을 이 데이터(609개)에도 재검증해서
  s 팁근처/고각도 문제가 재구성으로도 개선되는지 확인할 것. 체크포인트는 이번
  결과로 덮어씀(`models/position_segment_classifier_singleprobe_beta0180_4seg.pth`,
  29번 검증 이후 여기 다시 재학습 필요), 로그는
  `현서/scripts/contact_scenarios/fea/train_run_20260914_23densify_609_seed43.log`.

**🔜 2026-09-14(27번) |phi|>=90 Fx_board 전용 - "실패한 조합만 재시도" 스크립트
작성 완료, 실행은 다른 컴퓨터에서 할 것(이 컴퓨터는 위 30번에서 병렬화해서 실행 중).**
23번에서 "급하게 손대지 말 것"이라 미뤄뒀던
접근을 이제 구현함: `retry_highphi_missing_matv2.py`(신규) - 목표 격자(유효조합 42개 x
s 19개=798개, `run_highphi_sdensify_matv2_sweep.sh`와 동일한 LM_LIST/PHI_LIST/BAD_COMBOS
+ 기존 s격자와 오프셋격자 합집합)에서 **아직 실측 데이터가 없는 (L_M,phi,s) 조합을
데이터 파일에서 직접 계산**해서 뽑아낸 뒤, 기본 자동감쇠(stabilize=True, 이미 실패한
설정) 대신 **명시적 STABILIZE 계수(`--stabilize`, 기본 0.01)로 재시도**함.

**현재 확인된 숫자(이 컴퓨터 기준)**: 798개 목표 중 **기존 성공 265개, 재시도 대상
533개**. 케이스 하나에 약 15~20분 걸려서(스레드4개), 533개 전부 돌리면 100시간 넘음 -
**`--max-attempts`로 이번 실행에서 시도할 개수를 제한**하도록 만들어둠(예: 100개=하룻밤
분량). 이미 성공한 건 재계산 시 자동으로 빠지므로, 여러 번에 나눠 돌려도 체크포인트
관리 없이 이어서 하면 됨.

**다른 컴퓨터에서 실행**:
```bash
git pull origin main
cd 현서/scripts/contact_scenarios/fea
python retry_highphi_missing_matv2.py --dry-run              # 먼저 목표/누락 개수만 확인
python retry_highphi_missing_matv2.py --max-attempts 100     # 실제 재시도 (하룻밤 분량 예시)
```
**주의(Windows+conda 환경)**: `ccx`가 PATH에 없으면 죽으니 먼저
`C:\<conda경로>\Library\bin` 등을 PATH에 추가할 것(아래 "환경" 섹션 참고). 완료되면
`train_segment_classifier_singleprobe_beta0180_4seg.py` 재학습 후 `_diag_holdout_phi_breakdown.py`
로 |phi|>=90 Fx_board(기준값 0.440, 25/26번 참고)가 개선됐는지 확인할 것. STABILIZE=0.01이
효과 없으면 `--stabilize` 값을 0.002~0.05 사이에서 바꿔가며 재시도해볼 것(아직 검증 안 된
값, 임의로 고름).

**✅ 2026-09-14(26번) FORCE_LOSS_WEIGHTS 재검증 — 폐기, 원래(0.1)로 복귀.** 25번 체크포인트의
대체모델 5-fold R^2를 보니 Fy_total_N(=Fx_board)이 2026-08-19 당시 진단(-0.01, 노이즈)과
달리 이제 0.649로 나옴(데이터가 그동안 518개로 늘어서) — "노이즈라 못 배운다"는 옛 전제로
이 축 loss를 1/10로 깎아둔 `FORCE_LOSS_WEIGHTS=[0.1,1.0]`이 배울 수 있는 신호를 스스로
억누르고 있었을 가능성이 있어 재검증(시드43, 파인튜닝 없음 기준):

| 가중치 | balanced acc | s R²/MAE | Fx_board(전체) | **|phi|>=90 Fx_board** | Fy_board |
|---|---|---|---|---|---|
| **0.1(원래, 시드2개 검증됨)** | 83.0% | **0.910/5.77mm** | 0.626 | 0.440 | **0.855** |
| 0.5(단일실행) | 83.1% | 0.883/6.26mm | 0.723 | **0.477** | 0.673 |
| 1.0(단일실행) | **86.3%** | 0.915/5.95mm | 0.715 | 0.382 | 0.704 |

**결론**: 가중치를 올릴수록 목표(|phi|>=90 Fx_board)가 단조롭게 좋아지지도 않고(0.5>0.1>1.0),
대신 Fy_board(원래 제일 안정적이던 축)만 계속 나빠짐 - trunk 공유로 인한 멀티태스크
트레이드오프. 0.5/1.0은 시드 1개뿐이라 이 파이프라인에서 이미 확인된 시드 노이즈 폭
(Fy_board가 같은 세팅에서도 0.608~0.855까지 흔들림, 25번 참고) 안에 들어갈 수 있어
신뢰 못 함 - 더 결론 내리려면 시드를 더 늘려야 하는데 그 정도 컴퓨팅을 쓸 근거가 약함.
**가중치 튜닝은 여기서 폐기하고 0.1(시드2개로 검증된 유일한 설정)로 복귀함**(코드/체크포인트
둘 다 복원 완료). |phi|>=90 문제는 loss weight가 아니라 **그 구간 실측 FEA를 더 모으는
원래 처방**으로 넘어갈 것. 로그: `현서/scripts/contact_scenarios/fea/train_run_20260914*.log`.

**✅ 2026-09-11(25번) L_M/phi를 예측 대상에서 빼고 known input으로 바꿈 — |phi|>=90
Fx_board 문제 사실상 해결, config_head/lm_zero_head 통째로 제거.** 교수님 피드백("phi는
조종자가 거는 외부자기장 방향인데 왜 굳이 센서로 추정하냐"): phi_deg는 `force_model.py`
정의상 "외부자기장 방향"(제어입력), L_M도 그 로봇의 고정 MOM 위치 스펙이라 둘 다 실제
운용에서는 이미 아는 값 — B-field에서 추정해야 할 미지수가 아님. `SingleProbeClassifier`가
이 둘을 `config_head`로 "예측"하던 걸 없애고, `forward(x_img, config)`처럼 정규화된
(L_M,phi)를 trunk에 직접 입력으로 넣도록 구조 변경(`train_segment_classifier_singleprobe_beta0180_4seg.py`).
L_M=0 회귀 불안정을 우회하려고 8번(아래)에서 넣었던 `lm_zero_head`도 L_M을 더 이상
추정할 필요가 없어지면서 같이 제거됨.

CPU로 시드 2개(42,43) × 파인튜닝 유무 총 4회 정식 재학습(150k/60epoch)해서 교차검증:

| 조건 | balanced acc | s R²/MAE | Fx_board(전체) | **|phi|>=90 Fx_board** | Fy_board |
|---|---|---|---|---|---|
| 이전 기준값(24번 이전, known-input 이전) | 80.2% | 0.871/6.49mm | 0.600 | 0.355 | 0.877 |
| 시드42, 파인튜닝(2단계) 없음 | 85.1% | 0.908/5.80mm | 0.607 | 0.336 | 0.608 |
| **시드43, 파인튜닝 없음 — 최종 채택** | **83.0%** | **0.910/5.77mm** | **0.626** | **0.440** | **0.855** |
| 시드42/43, 파인튜닝(2단계, 실측 419개) 적용 | 69.7~70.3%(↓) | 0.806~0.829(↓) | 0.486~0.498(↓) | 0.432~0.530(↑) | 0.509~0.609(↓) |

**결론**: (1) L_M/phi를 known input으로 주는 것만으로 balanced acc/s/Fx_board가 두 시드
모두 일관되게 기존보다 좋아짐 — 교수님 지적이 맞았음. (2) 시드42의 Fy_board=0.608
하락은 시드43(0.855)에서 정상 회복돼 **순수 시드 노이즈로 확정**(기존에도 반복 확인된
패턴). (3) **24번의 2단계 파인튜닝은 |phi|>=90 Fx_board는 더 올리지만(0.336→0.432,
0.440→0.530) balanced acc/s/Fy_board를 양쪽 시드 모두 일관되게 깎아먹음**(실측
419개뿐이라 과적합으로 추정) — **`FINETUNE_ON_REAL=0`(기본 끔)을 권장, 24번의 파인튜닝
레버는 사실상 폐기**. **최종 채택 체크포인트**: 시드43+파인튜닝없음
(`models/position_segment_classifier_singleprobe_beta0180_4seg.pth`에 저장됨, 다른
조합들은 `models/*_knowninput_*.pth`로 백업 보관). 로그는
`현서/scripts/contact_scenarios/fea/train_run_20260911*.log` 4개 참고.

**🔜 다음 세션**: (a) 이 체크포인트로 커밋할지 확인받을 것(아직 커밋 안 함). (b) 여기
반영 안 된 진단 스크립트 6개(`_diag_lm_by_value_breakdown.py`, `_diag_lm_holdout_error.py`,
`plot_final_scatter_summary.py`, `_diag_force_geom_reconstruction.py`,
`_diag_holdout_phi_breakdown.py`, `_diag_pipeline_leakage.py`)는 여전히 옛 5-output
`SingleProbeClassifier`를 복제해서 쓰고 있어 이 새 체크포인트를 못 읽음 - 필요할 때
같은 패턴(known input)으로 같이 고칠 것. (c) `_diag_force_geom_reconstruction.py`(18번)의
"진짜 phi 넣으면 R²=0.749" 재구성 아이디어를 이제 phi가 항상 known input이니 재검증해볼
가치 있음(더 이상 "예측된 phi의 오차"가 없으므로).

**⚠️ 2026-08-27(10차): HIGH_PHI_WEIGHT=1.5 재학습 — 뚜렷한 개선 없음, 이 레버는 폐기
권장.** 20번 실행(시드=42, 커밋된 체크포인트와 동일 시드로 순수 비교) - **|phi|>=90
구간 Fx_board R²=0.355**(가중치 3.0일 때 0.343과 사실상 동일, 개선 없음). 다른 지표는
무난함(|phi|<90 Fx_board 0.782로 오히려 개선, 전체 balanced acc 80.2%/Fy_board 0.848/
L_M 하이브리드 R²=0.809). **⚠️ 다만 19번에서 같은 파이프라인의 시드 하나 차이로
Fx_board R²가 0.145~0.581까지 튄다는 걸 이미 확인했기 때문에, 0.343→0.355라는 이번
차이가 가중치 때문인지 순수 시드 범위 안인지 단일 실행만으로는 확정 못 함** — 그래도
최소한 "뚜렷한 개선은 없다"는 결론은 유지 가능. **🔜 다음 세션: HIGH_PHI_WEIGHT
레버는 여기서 접고(3.0, 1.5 둘 다 목표 구간을 못 고침), phi=90~150 실측 FEA를 더
늘리는 원래 검증된 방법으로 넘어갈 것** — 22번(s 오차 세분화) 결과도 함께 참고.

**🔜🔜 지금 바로 실행할 것 (23번, 신규)**: s=60-100mm(팁 근처) 오차가 큰 문제는
|phi|>=90 문제와 무관하게 저-phi 구간에도 있는데, 그쪽은 s=60-100mm에 10mm 간격
격자(60,70,80,90,100)밖에 없어서 성김 - 오프셋 격자(65,75,85,95)로 조밀화하는 스윕을
새로 만들어뒀음. 아래 그대로 실행:
```bash
cd 현서/scripts/contact_scenarios/fea
bash run_lowphi_tipdensify_matv2_sweep.sh
```
(phi in {0,+-30,+-60} x L_M 8개 = 40조합 x s 4개 = 160케이스, ~1-2시간 예상 - 이전
run_highphi_sdensify_matv2_sweep.sh와 같은 방식이라 성공률도 비슷할 것으로 예상,
저-phi라 torque-zero 특이점 문제가 없어서 오히려 성공률은 더 높을 수도 있음). 끝나면
`train_segment_classifier_singleprobe_beta0180_4seg.py` 재학습 → `_diag_s_breakdown.py`로
s=60-100mm MAE(현재 9.49mm)가 줄었는지 확인. **|phi|>=90 Fx_board 문제(20번)는 이미
그 구간을 s=15~95 전체 오프셋 격자로 조밀화 완료한 상태라 같은 방식으로 더 늘리는 건
효과가 낮을 수 있음 - 이건 다음 세션에서 "실패한 특정 조합만 재시도" 같은 다른 접근이
필요할 수 있으니 급하게 손대지 말 것.**

**🔜🔜🔜 지금 바로 실행할 것 (24번, 신규, 2026-09-07)**: `train_segment_classifier_singleprobe_beta0180_4seg.py`에
코드 변경 2개 추가함(CPU 소규모 드라이런으로 실행 자체는 검증 완료, 실제 효과는 미검증 -
GPU로 정식 재학습해서 판단 필요):
1. **s 공간가중치**: s=40mm부터 100mm까지 최대 3배로 부드럽게 커지는 loss 가중치 추가
   (`s_spatial_weight()`, 22/23번의 s=60-100mm MAE=9.49mm 문제 대상)
2. **실측 데이터 파인튜닝(2단계 학습, 신규 레버)**: 지금까지 최종 CNN은 150k 합성데이터로만
   학습되고 실측 FEA는 검증에만 쓰였다는 걸 발견 - 실측이 gradient를 한 번도 직접 업데이트한
   적이 없었음. 합성데이터 학습(1단계) 후 실측 fit_rows로 짧게(15epoch, lr=5e-5) 추가
   파인튜닝(2단계)하는 걸 추가함. `|phi|>=90` 실측 샘플은 4배 오버샘플링해서 Fx_board
   고각도 문제에 더 집중시킴. 학습 로그에 1단계/2단계 실측 홀드아웃 성능이 각각 찍히니
   직접 비교 가능(`FINETUNE_ON_REAL=0`으로 끄면 기존 방식으로 복귀).

**바로 실행**:
```bash
cd 현서/scripts/contact_scenarios/fea
python train_segment_classifier_singleprobe_beta0180_4seg.py
```
**확인할 것**: 로그의 "[2단계(실측 파인튜닝 후, 신규)]" 블록에서 |phi|>=90 Fx_board
R²(기준값 0.355)와 s>=60mm MAE(기준값 9.49mm)가 "[1단계(파인튜닝 전)]" 대비 실제로
좋아졌는지. 다른 지표(balanced acc, Fy_board, L_M 하이브리드)가 파인튜닝 때문에
악화되지 않았는지도 같이 볼 것 - 특히 fit_rows가 419개뿐이라 과적합(합성-val 대비
실측만 좋아지고 일반화는 나빠지는 패턴) 가능성을 경계할 것. 둘 다 도움 안 되면
`S_WEIGHT_MAX=1.0 FINETUNE_ON_REAL=0`으로 꺼서 이전 상태로 되돌리고 23번 스윕 결과를
기다릴 것.

**✅ 2026-08-27(6차): L_M=0 원인 규명 사실상 완료 — "분류는 완벽, 회귀만 실패".**
14번(다변량 식별가능성 테스트) 결과 **L_M=0 vs 12.5mm 구분 정확도 99.3%**로 오히려
"잘 되는" 구간(94~97%)보다 높음 — 다변량 문제 가설도 기각. 지금까지 쌓인 증거(11번:
신호 있음+대체모델 배움, 14번: 다변량으로도 완벽 구분)를 종합하면 **L_M=0 근처는
"0인지 아닌지 분류"는 쉬운데 "정확히 몇 mm인지 연속 회귀"만 유독 실패하는 상태** —
신호의 L_M 민감도(기울기)가 그 근처에서 극단적으로 작기 때문으로 추정(아래 14번 상세).

**✅ 2026-08-26(3차) 추가 발견 + 수정: 최종 CNN 학습에 시드 고정이 아예 없었음.** 같은
재학습에서 Fy_board R²가 0.857→0.648로 떨어진 게 보고됐는데, FEA 힘 값 자체는 재확보
전후로 거의 안 바뀜(Fx/Fy_total_N 변화량 평균 0.00001mN 수준, 무시 가능)이라 데이터
문제가 아님을 확인함. 원인 조사 중 `train_segment_classifier_singleprobe_beta0180_4seg.py`의
**최종 CNN(SingleProbeClassifier) 학습에는 `torch.manual_seed`가 전혀 없었다는 걸 발견**
(대체모델 앙상블만 `seed=i`로 고정돼있었음) — 가중치 초기화와 DataLoader shuffle이 매
실행마다 완전히 랜덤이라, **지금까지 "이 변경 때문에 지표가 오르내렸다"고 해석해온 여러
회차 비교(HIGH_PHI_WEIGHT, mom_* 추가 등)에 실제로는 순수 시드 노이즈가 얼마나
섞여있었는지 알 수 없는 상태였음**. `torch.manual_seed(42)` + `torch.cuda.manual_seed_all(42)`를
모델 생성 직전에 추가해 수정 완료 — **다음 재학습부터는 회차 간 비교가 처음으로
"시드 노이즈 없이 순수하게 코드/데이터 차이만" 비교하는 게 됨.** 이번 Fy_board 하락이
진짜 mom_* 추가 때문인지 순수 시드 운이었는지는, 이 수정 이후 재학습해서 다시 확인해야 함.

**5번(원인 조사) 결론**: `_diag_holdout_phi_breakdown.py`로 파봤더니, 실측 홀드아웃을
뽑던 코드가 `rng(42).permutation(len(all_rows))`로 "전체 개수의 랜덤 20%"를 뽑는
방식이었는데, **데이터 개수가 바뀌면(404→518) 같은 시드를 써도 뽑히는 행 자체가
완전히 달라짐** — 그래서 지난 홀드아웃(n=80)과 이번 홀드아웃(n=103)은 애초에 서로 다른
검증셋이라 직접 비교가 안 맞았음. 실제로 쪼개보니: |phi|<90(쉬운 구간)만 보면 여전히
R²=0.64로 정상, |phi|>=90(어려운 구간, 원래도 R²가 낮았던 구간)은 기존행도
R²=0.39로 원래 낮았음 - 새 데이터가 나쁜 게 아니라 **고각도 비중이 43%→56%로 늘면서
랜덤 홀드아웃에도 어려운 구간이 더 많이 뽑혀 평균이 끌려 내려간 것**. `Fx_board 0.729→0.462
"악화"는 대부분 측정 착시`. **수정 완료**: 홀드아웃을 "전체의 랜덤 20%" 대신 **행
고유키(L_M,phi,beta,s)를 해시해서 결정**하도록 `train_segment_classifier_singleprobe_beta0180_4seg.py`와
관련 진단 스크립트 5개(`_diag_surrogate_only.py`, `_diag_pipeline_leakage.py`,
`_diag_surrogate_feature_test.py`, `_diag_forcemodel_vs_fea_displacement.py`,
`_diag_holdout_phi_breakdown.py`) 전부 수정함 — 이제 데이터가 늘어도 기존 행의
홀드아웃 소속이 절대 안 바뀜(404→518 검증: 기존 404개 중 소속 바뀐 행 0개, 확인 완료).
**⚠️ 주의: 지금 저장된 체크포인트는 옛날(버그 있는) 분할로 학습된 거라, 새 분할 기준으로
평가하면 "홀드아웃"이라 부르는 행 중 일부를 실제로는 학습에 썼을 수 있어 숫자가 부풀려짐
(리키지) — 반드시 재학습부터 하고 나서 나온 숫자를 써야 함.**

1. ~~[확인] `git log`로 `5a2b9c4`(3구간 원복) 이후 상태 확인~~ — 완료, 정상 상태였음.

2. ~~실측 FEA 데이터 늘리기 (L_M 조밀화)~~ — **완료**: `run_lm_densify_matv2_sweep.sh`
   380케이스 실행(6개 오답-branch 조합 제외, STABILIZE 기본 적용), **158/380 성공(41.6%),
   8.6시간 소요**. `fea_lm_phi_pos_matv2_all.json`: 244개 → **402개**.
   대체모델 단독 홀드아웃(`_diag_surrogate_only.py`, n=80) 재확인: tip_ux R²
   0.555→**0.724**, tip_uy 0.877→0.845(둘 다 0.8 근처/이상 유지), 로컬Fy(=Fx_board) R²
   -0.01→**0.486**(대체모델 자체가 이 타겟을 처음으로 유의미하게 배움).

3. ~~데이터 쌓이면 재학습 → 검증~~ — **완료, 목표 달성**: `train_segment_classifier_singleprobe_beta0180_4seg.py`
   재실행(150k/60epoch, GPU 25.2분). **순수 실측 FEA 홀드아웃(n=80) 힘 R² 대폭 개선**:
   Fx_board(=로컬Fy, 원래 최악이던 타겟) **0.009→0.729**, Fy_board(=로컬Fx) **0.337→0.646**.
   구간분류(73.4%→73.9%)/s(0.819→0.820)는 이미 좋았어서 변화 미미 - 이번 라운드는 힘 추정
   전용으로 효과 있었음. 상세 수치는 아래 표 참고.

4. ~~phi=90~150 데이터 추가 확보~~ — **완료**:
   - `run_stabilize_pilot_matv2.sh`(명시적 STABILIZE 감쇠계수 3종, 60케이스): 2/60(3.3%)만
     성공, 효율 낮아 폐기(60개 중 2개만 병합, 404개).
   - `run_highphi_sdensify_matv2_sweep.sh`(s격자 오프셋 9개, 378케이스): **114/378
     성공(30.2%)**, 404개 → **518개**.
   - 대체모델 단독 홀드아웃(`_diag_surrogate_only.py`, n=103) 재확인: tip_ux R² 0.809,
     tip_uy R² 0.933(둘 다 유지/개선), 로컬Fy(=Fx_board) R²=0.493(0.486에서 거의 그대로,
     개선 안 됨 — 아래 5번과 연결).
   - `train_segment_classifier_singleprobe_beta0180_4seg.py` 재실행(150k/60epoch, GPU
     25.3분). **순수 실측 FEA 홀드아웃(n=103) 결과가 엇갈림**: 구간분류 73.9%→**75.8%**,
     s R² 0.820→**0.839**(둘 다 개선), Fy_board(=로컬Fx) 0.646→**0.750**(개선)인데
     **Fx_board(=로컬Fy) 0.729→0.462로 악화**. 상세 수치는 아래 표 참고.

5. ~~Fx_board(=로컬Fy) 회귀만 악화된 원인 조사~~ — **완료, 위 결론 참고** (홀드아웃
   측정 버그였음, 모델/데이터 문제 아니었음).

6. ~~고친 홀드아웃 로직으로 재학습~~ — **완료(2026-08-25)**: `train_segment_classifier_singleprobe_beta0180_4seg.py`
   재실행(150k/60epoch, GPU 15.4분). **순수 실측 FEA 홀드아웃(n=99, 해시기반 고정분할) —
   이제부터의 진짜 기준선**: 구간분류 acc=76.8%/balanced acc=**78.7%**, s R²=**0.899**,
   Fx_board(=로컬Fy) R²=**0.661**, Fy_board(=로컬Fx) R²=**0.747**. 지금까지 표에 있는 이전
   숫자(n=36/80/103)는 전부 다른 분할 기준이라 이 새 숫자와 직접 비교하지 말 것 — **여기서부터
   새로 비교 시작.** 상세 수치는 아래 표 참고.

7. ~~L_M/phi(config_head) 실측 홀드아웃 검증 추가~~ — **완료, 신규 문제 발견**: 재학습
   (150k/60epoch, GPU 15.7분) 로그에 L_M/phi 실측 검증 처음 출력됨(n=99). **L_M_mm
   R²=0.565**(합성-val 0.895와 큰 격차 — 힘 추정 때 겪은 "합성-val 낙관/실측 저조"
   패턴이 여기도 있음, MAE=10.10mm). **phi_deg R²=0.928**(합성-val 0.902와 비슷/오히려
   나음, 문제 없음). L_M만 유독 나쁜 원인 미조사 — 아래 10번 참고.

8. ~~|phi|>=90 힘 손실 가중치 3배 실험~~ — **완료, 결과 엇갈림**: (7번과 같은 재학습에
   포함, `HIGH_PHI_WEIGHT=3.0`). 우려했던 "balanced acc 하락" 리스크는 **안 일어남**
   (78.7%→**83.9%** 오히려 개선). Fy_board도 개선(0.747→**0.857**). 그런데 **Fx_board는
   0.661→**0.601**로 하락** — `_diag_holdout_phi_breakdown.py`로 쪼개보니 |phi|>=90
   구간 Fx_board R²=**0.343**(가중치를 3배 줬는데도 여전히 약함), |phi|<90은 Fx_board
   R²=0.731/Fy_board R²=0.824로 정상. 결론: 이 레버는 Fy_board/balanced acc엔 확실히
   도움됐지만 Fx_board(=로컬Fy, 원래도 가장 약한 타겟)의 고각도 구간 문제는 손실
   가중치만으로는 안 풀림 — `HIGH_PHI_WEIGHT` 값 재튜닝(낮춰서 재시도) 또는 실측 FEA
   추가가 다음 후보. 상세 수치는 아래 표 참고.

9. **급하지 않음, 참고만**: `EN_FACTOR=2.0`(K1=2×K2) 가정의 정확한 근거는 아직 불명(논문도
   Section IV-A에서 K1이 부정확하다고 자인함). 와이어 보강 때문은 아닌 걸로 결론남(아래 참고).

10. ~~L_M=0/12.5mm MOM 실측값 재확보~~ — **완료, 그러나 문제 해결 안 됨**:
    `run_lm_near_zero_mom_refetch.sh`(24조합, 159케이스) 실행 → **128/159 성공(80.5%,
    이전 라운드들보다 훨씬 높음 — L_M=0/12.5mm는 토크제로 특이점에서 멀어 FEA가 잘 풀리는
    구간)** → `fea_lm_phi_pos_matv2_all.json` 518개(교체 128, 신규 0, 개수 그대로) →
    재학습(150k/60epoch, GPU 15.6분). 대체모델 단독: mom_ux_avg_mm R²=0.769,
    mom_uy_avg_mm R²=0.872(둘 다 잘 배움, 근사식 문제였다면 여기서 개선됐어야 함 - 실제로
    개선됨). 전체 파이프라인 실측 홀드아웃(n=99): L_M_mm R²=0.565→**0.669**로 얼핏
    개선돼 보이지만, **`_diag_lm_holdout_error.py`로 L_M=0만 따로 떼어보면(n=16)
    MAE=40mm→**46.52mm로 오히려 악화**, 예측값이 9~70mm로 넓게 흩어짐(전체 평균
    39.8mm 근처로 수렴하는 패턴 — 사실상 못 배우고 있다는 신호)**. L_M≠0은 R²=0.932로
    여전히 훌륭함(문제 없음). **결론: "frac 근사가 신호를 지웠다"는 가설은 틀렸거나
    불충분함** — MOM 실측값 자체는 대체모델이 잘 배우는데도 최종 B-field 기반 예측은
    안 됨 → 원인이 근사식이 아니라 **L_M=0 근처에서 B-field가 물리적으로 L_M을 구분할
    정보를 안 담고 있을 가능성**으로 가설 전환. 부수 관찰(확정 아님, run-to-run 변동
    가능성 배제 못 함): 같은 재학습에서 Fy_board R²가 0.857→0.648로 하락.

11. ~~L_M=0 근처 B-field 물리적 구분 가능성 직접 확인~~ — **완료(양쪽 컴퓨터에서 독립적으로
    확인, 같은 결론)**: "물리적으로 원리상 구분 불가"라는 가설은 **기각**.
    - **(각도별 확인)** L_M=0 vs 12.5mm 실측 MOM 변위 기준 델타-B field를 phi별로 비교:
      **phi=0 근처(비율 0.01~0.07)만 거의 구분이 안 되고, 나머지 대부분의 phi(±30~±150)는
      비율 1.4~1.7로 충분히 구분 가능**(비율 1.0 이상 = 신호차이가 평균신호크기만큼 큼).
    - **(전체 L_M 구간 비교, `_diag_lm_bfield_distinguishability.py`)** (phi,beta,s)가
      일치하는 인접 L_M쌍(12.5mm 간격)마다 구분신호를 비교:
      | L_M 구간 | 매칭쌍 | 구분신호(uT) | 자체크기(uT) | 상대비율 |
      |---|---|---|---|---|
      | 0.0-12.5 | 51 | 1.756 | 1.275 | 1.339 |
      | 12.5-25.0 | 40 | 2.429 | 1.491 | 1.486 |
      | 25.0-37.5 | 36 | 1.805 | 1.319 | 1.295 |
      | 37.5-50.0 | 42 | 1.896 | 1.375 | 1.377 |
      | 50.0-62.5 | 27 | 2.562 | 2.124 | 1.081 |
      | 62.5-75.0 | 28 | 1.959 | 2.093 | 0.892 |
      | 75.0-87.5 | 22 | 1.035 | 1.673 | 0.646 |

      **0-12.5 구간이 다른 구간보다 특별히 작지 않음(오히려 62.5-75, 75-87.5보다 큼).**
    - **(대체모델 확인, `_diag_mom_surrogate_by_lm.py`)** mom_ux/uy 예측이 이 구간에서
      유독 나쁜지 확인 — MAE가 거의 0으로 문제 없어 보임(표본 27개, 확정적이진 않음).
    - **(L_M값별 실측 홀드아웃 오차 세분화, `_diag_lm_by_value_breakdown.py`)** 전체 데이터
      개수는 L_M값마다 60~75개로 비슷함(데이터 "양"의 문제 아님)인데도:
      | L_M | n | MAE |
      |---|---|---|
      | **0mm** | 16 | **32.0mm** |
      | 12.5mm | 15 | 8.7mm |
      | 25mm | 10 | 4.0mm |
      | 37.5mm | 10 | 2.7mm(최고) |
      | 50mm | 19 | 4.6mm |
      | 62.5mm | 6 | 3.0mm |
      | 75mm | 17 | 6.2mm |
      | 87.5mm | 6 | 7.5mm |

      가운데(25~62.5mm)는 다 좋고 양쪽 끝으로 갈수록 나빠지는 전형적 회귀모델의 "범위
      경계 효과"인데, **L_M=0은 그 경계효과를 훨씬 넘는 수준**(반대쪽 끝 87.5mm의 4배 이상).

    **종합 결론**: 신호도 있고(phi=0 근처만 예외) 대체모델도 mom_*를 잘 배우는데,
    **최종 CNN(config_head)만 L_M=0에서 실패** — 원인 미확정, 유력 후보는 아래 14번
    (phi/s가 같이 변하는 전체 분포에서 L_M=0이 상대적으로 약한 신호라 다변량 식별이
    어려운 조건부 문제) 또는 **L_M=0이 K1(베이스~MOM) 구간 길이가 정확히 0이 되는
    구조적 특이점이라 회귀보다 별도 구간 분류가 맞는 목표일 가능성**.

12. ~~최종 CNN 시드 고정 안 돼있던 문제 발견 + 수정~~ — **완료(2026-08-26)**: 위 결론
    참고. `torch.manual_seed(42)`/`torch.cuda.manual_seed_all(42)` 추가 완료
    (`model = SingleProbeClassifier()` 바로 앞).

13. ~~시드 고정 상태로 재학습~~ — **완료(2026-08-26, 5차)**: 150k/60epoch, GPU 15.9분.
    실측 홀드아웃(n=99): 구간분류 balanced acc 83.9%, s R²=0.887, Fx_board R²=0.638,
    **Fy_board R²=0.767**(이전 0.648보다 개선 — **이전 하락 상당 부분이 순수 시드
    노이즈였음을 뒷받침**, 다만 이전 최고치 0.857까지는 회복 안 됨, 시드 1개만으론
    완전히 못 가름), L_M_mm R²=0.714(이전 0.669보다 소폭 개선), phi_deg R²=0.939.
    **`_diag_lm_by_value_breakdown.py`로 L_M별 MAE 재확인**: L_M=0만 MAE=**26.3mm**
    (직전 32.0mm와 비슷한 수준, 나머지 구간 2.9~6.5mm 대비 여전히 4~9배 나쁨) —
    **시드를 바꿔도 재현됨 = 시드 노이즈가 아니라 진짜 구조적 문제로 확정.** 상세 수치는
    아래 표 참고.

14. ~~L_M=0 다변량 식별가능성 직접 테스트~~ — **완료(2026-08-27), 가설 기각 — 결정적 결과**:
    `_diag_lm_multivariate_identifiability.py`(신규) — phi/s가 자연분포 그대로(짝짓지
    않음) 섞인 실측 delta-B로 L_M=0 vs 12.5mm를 구분하는 로지스틱 회귀를 5-fold
    교차검증:
    | 비교 | 5-fold 정확도 |
    |---|---|
    | **L_M=0 vs 12.5mm(문제 구간)** | **99.3%** |
    | L_M=25 vs 37.5mm(잘 되는 구간) | 97.2% |
    | L_M=50 vs 62.5mm(잘 되는 구간) | 94.9% |

    **L_M=0 구간이 "잘 되는" 구간들보다 오히려 더 잘 구분됨** — 다변량 식별 문제 가설도
    기각. **종합 결론(11+14번 다 합쳐서)**: 신호도 있고, 대체모델도 배우고, 심지어
    "이게 0이냐 아니냐" 분류는 99% 완벽한데, **"정확히 몇 mm냐"는 연속값 회귀만 실패**.
    원인 추정: L_M=0 근처는 신호의 L_M에 대한 민감도(기울기)가 극단적으로 작아서
    (L_M=0→2.5mm 신호변화가 L_M=40→60mm의 1/20 수준, 2026-08-26 확인) 분류(경계
    하나만 찾으면 됨)는 쉬워도 회귀(정밀한 크기까지 맞춰야 함)는 어려움 - 자로
    "1cm보다 크냐"는 눈으로 알아도 "정확히 1.0mm냐 1.3mm냐"는 훨씬 정밀해야 하는
    것과 같은 이치.

16. ~~L_M 2단계 구조(분류+회귀) 구현~~ — **완료(2026-08-27, 코드만, 재학습 전)**:
    `train_segment_classifier_singleprobe_beta0180_4seg.py`에 `lm_zero_head`(이진분류,
    "L_M<6.25mm인가") 추가:
    - `SingleProbeClassifier.forward()`가 이제 **5개**를 반환함(기존 4개 + lm_zero_logit) -
      이 클래스를 복사해 쓰는 진단 스크립트 6개(`_diag_lm_by_value_breakdown.py`,
      `_diag_lm_holdout_error.py`, `plot_final_scatter_summary.py`,
      `_diag_force_geom_reconstruction.py`, `_diag_holdout_phi_breakdown.py`,
      `_diag_pipeline_leakage.py`)도 전부 같이 수정함(다른 오래된 실험 스크립트는
      안 건드림 - 필요해지면 그때 같은 패턴으로 고칠 것).
    - `LM_ZERO_THRESHOLD_MM = 6.25`(실측 격자 0mm와 12.5mm의 중간값)로 라벨 생성,
      `lm_zero_all = (|L_M| < 6.25)` - worker() 안 건드리고 c_all에서 사후 계산.
    - 학습 루프에 `BCEWithLogitsLoss` 추가, 체크포인트에 `lm_zero_threshold_mm` 저장.
    - **하이브리드 L_M 재구성**: `hybrid_lm_report()` 함수 - lm_zero_head가 "0에 가깝다"고
      분류하면 대표값 0mm 사용, 아니면 기존 config_head 회귀값 그대로 사용. 합성-val과
      실측 홀드아웃 양쪽에 다 리포트하도록 추가함.
    - ⚠️ **로컬(CPU)에서 아키텍처/로직만 스모크테스트 완료**(forward shape, BCE loss,
      하이브리드 계산 로직 전부 수동 검증함) - 실제 150k 합성데이터로 재학습은 아직 안
      돌림. **기존 체크포인트는 lm_zero_head가 없어서 이 코드로 그대로 못 씀 - 재학습
      필수.**

17. ~~L_M 2단계 구조 재학습~~ — **완료(2026-08-27, 8차), L_M=0 사실상 해결 + 부작용
    발견**: 150k/60epoch, GPU 16.1분. `lm_zero_head` 분류 정확도 합성-val 99.0%/실측
    96.0%(예상대로 잘 됨). **하이브리드 L_M(실측 홀드아웃, n=99): 전체 MAE 5.41mm,
    L_M=0만 따로 보면 `_diag_lm_by_value_breakdown.py`에 추가한 구간별 비교로
    19.36mm(순수 회귀)→7.70mm(하이브리드)** — 다른 구간(2.7~7.9mm)과 비슷한 수준이 됨,
    **L_M=0 문제 사실상 해결.** L_M 전체 R²도 0.714(직전)→0.831로 개선, phi_deg
    R²=0.962(개선 유지).
    **⚠️ 그런데 같은 재학습에서 Fx_board_N R²가 0.638→0.145로 크게 하락**(MAE
    0.0048→0.0072mN), 구간분류 balanced acc도 83.9%→80.8%로 소폭 하락. Fy_board는
    0.767→0.871로 개선. Fx_board는 원래도 라운드마다 0.4~0.7대를 오가던 불안정한
    타겟이지만 이번 낙폭(약 0.5)은 그보다 훨씬 큼 — `lm_zero_head`가 공유 trunk의
    표현력을 힘 예측 쪽에서 가져간 부작용인지, 극단적 시드 변동인지 미확정. 아래
    19번 참고. 상세 수치는 아래 표 참고.

18. ~~F_mag+각도 기하학 재구성 재검증 (하이브리드 L_M 반영)~~ — **완료(2026-08-27),
    아직 시기상조로 재확인**: L_M이 오늘 좋아졌으니(하이브리드) `_diag_force_geom_reconstruction.py`를
    lm_zero_head 기반 하이브리드 L_M을 쓰도록 고쳐서 재검증함.
    | 조건 | Fx_board R² |
    |---|---|
    | 지금 직접회귀 | 0.268 |
    | 재구성(하이브리드 L_M + 예측 phi/s, 현실) | **-0.352** |
    | L_M만 진짜 | 0.629 |
    | **phi만 진짜** | **-1.085**(오히려 더 나쁨) |
    | s만 진짜 | -0.011 |
    | 셋 다 진짜(오라클) | 0.749 |

    L_M 하이브리드가 도움은 되지만(진짜 L_M 넣으면 0.629까지 회복), **phi가 여전히
    발목을 잡음** — 심지어 phi만 진짜로 바꿔도 더 나빠지는 비단조적 현상 확인(theta가
    L_M·phi의 비선형 조합이라 하나만 고치면 나머지 오차와 안 맞아 더 틀어질 수 있음).
    **결론: 지금 파이프라인을 이 재구성 방식으로 바꾸는 건 여전히 시기상조** — phi
    예측도 L_M만큼 더 좋아져야 승산이 있음. 코드(`_diag_force_geom_reconstruction.py`)는
    남겨뒀으니 나중에 phi 정확도가 더 오르면 재검증할 것.

19. ~~Fx_board 고각도(|phi|>=90) HIGH_PHI_WEIGHT 재튜닝~~ — **완료(2026-08-27), 재학습
    대기**: 18번(재구성)이 아직 시기상조로 확인돼서, 검증된 손실가중치 레버를 재시도.
    `HIGH_PHI_WEIGHT`를 3.0→**1.5**로 낮춤(3.0은 balanced acc/Fy_board는 개선했지만
    정작 목표였던 그 구간 Fx_board R²=0.343으로 여전히 약했음 - 가중치가 너무 세서
    그 구간 노이즈에 과적합했을 가능성). **재학습 필요(아래 20번)** — 결과 확인 후
    1.5도 효과 없으면 이 레버 자체를 폐기하고 실측 FEA 추가로 넘어갈 것.

20. ~~HIGH_PHI_WEIGHT=1.5 재학습~~ — **완료(2026-08-27, 10차), 뚜렷한 개선 없음**:
    150k/60epoch, GPU 16.0분, `FINAL_SEED` 미지정(기본값 42, 커밋된 체크포인트와 동일
    시드로 순수 비교). `_diag_holdout_phi_breakdown.py`로 확인: **|phi|>=90 Fx_board
    R²=0.355**(가중치 3.0일 때 0.343과 사실상 동일 — 개선 없음). |phi|<90 Fx_board는
    0.782로 오히려 좋아짐, Fy_board 0.848/balanced acc 80.2%/L_M 하이브리드 R²=0.809는
    무난. **⚠️ 해석상 주의**: 21번(같은 파이프라인, 시드 하나 차이로 Fx_board R²가
    0.145~0.581까지 요동침을 확인)을 감안하면, 0.343→0.355 차이가 가중치 효과인지
    순수 시드 노이즈인지 이 실행 하나로는 확정 못 함 — 그래도 "뚜렷한 개선은 없다"는
    결론은 유지 가능. **19번 계획대로 이 레버(HIGH_PHI_WEIGHT 튜닝)는 폐기, 실측 FEA
    추가 쪽으로 넘어갈 것.**

21. ~~Fx_board 급락(0.638→0.145) 원인 확인~~ — **완료(2026-08-27, 9차), 순수 시드
    노이즈로 확정**: `train_segment_classifier_singleprobe_beta0180_4seg.py`에
    `FINAL_SEED` 환경변수 추가(기본값 42, 하위호환), `FINAL_SEED=43`으로 재학습(진단용,
    체크포인트는 커밋 안 함 - `git checkout`으로 시드=42 원본 복원함):
    | 시드 | Fx_board R² | L_M=0 순수회귀 MAE | L_M=0 하이브리드 MAE | lm_zero_head 정확도 |
    |---|---|---|---|---|
    | 42(커밋된 버전) | 0.145 | 19.36mm | **7.70mm** | 96.0% |
    | 43(진단용) | **0.581** | 27.05mm | 17.25mm | 93.9% |

    **Fx_board는 시드=43에서 정상 범위(0.581)로 돌아옴 — 급락은 새 헤드의 부작용이
    아니라 순수 시드 노이즈로 확정.** 다만 **L_M=0 하이브리드 개선의 절대 크기도
    시드마다 크게 다름**(7.70mm vs 17.25mm) — 방향(하이브리드가 순수회귀보다 항상 나음)은
    재현되지만 "완전 해결"이라 부르기엔 이름. 앞으로 이 파이프라인 결과를 볼 때는
    **단일 시드 결과를 확정으로 받아들이지 말고, 방향성(개선/악화)에 무게를 두고 절대
    수치는 참고 정도로 볼 것** — 필요하면 여러 시드 평균을 내는 것도 고려(급하지 않음).

22. ~~s(접촉위치) 예측 오차 세분화~~ — **완료(2026-08-27)**: `_diag_s_breakdown.py`(신규)로
    실측 홀드아웃(n=99) s 오차를 phi/s값/구간경계/L_M별로 쪼갬. **결론: s는 Fx_board와
    다른 문제** — |phi|<90(MAE=6.54mm)과 |phi|>=90(MAE=6.17mm)이 거의 같아서, 18~21번의
    고각도 문제와는 무관함. 대신 **s값 자체가 클수록(팁 쪽, 60-100mm 구간) 오차가
    확 커짐**: s 0-20mm MAE=4.78mm, 20-40mm MAE=5.56mm, 40-60mm MAE=**2.88mm(최고)**,
    60-100mm MAE=**9.49mm(최악, n=34로 표본도 충분)** — 실제 s 최대값은 100mm이고
    구간분류 클래스가 `min(N_CLASSES-1,...)`로 마지막 구간을 clamp해서 60mm 이상이
    전부 "구간3"으로 묶임(구간 폭이 20mm가 아니라 40mm라 다른 구간과 단순비교 시 주의).
    옛 교훈(FEA는 s가 클수록/팁에
    가까울수록 지렛대효과로 신호가 약해져 수렴 실패율이 높았음, 484번 줄 근처 참고)과
    일치하는 패턴. (주의: 이 구간의 R²=0.020/-8.875 같은 극단값은 구간 내 실제값
    분산이 작아서 생기는 착시이니 MAE로 판단할 것.) 구간 경계(±3mm) 근처는 MAE=6.68mm로
    안쪽(6.16mm)보다 살짝만 나쁨(큰 문제 아님). L_M별로는 L_M=62.5/75mm가 유독
    나쁨(MAE=8.90/9.85mm) — 표본이 적어(n=6,17) 확정은 아니지만 위 s=60-100mm 문제와
    같은 원인(그쪽 L_M 조합에 고-s 데이터가 몰려있을 가능성)일 수 있음.
    **다음 후보 조치**: 아래 23번으로 구체화함.

23. **[신규, 2026-08-27] s=60-100mm 저-phi 조밀화 스윕 — 바로 실행할 것**: 22번에서
    찾은 s=60-100mm 문제가 |phi|>=90과 무관(저-phi에도 동일하게 있음)한데, 저-phi
    구간은 그 대역에 10mm 간격 격자(60,70,80,90,100)뿐이라 성김 - `run_highphi_sdensify_matv2_sweep.sh`와
    같은 방식(오프셋 격자)으로 `run_lowphi_tipdensify_matv2_sweep.sh`(신규)를 만들어둠.
    ```bash
    cd 현서/scripts/contact_scenarios/fea
    bash run_lowphi_tipdensify_matv2_sweep.sh
    ```
    phi in {0,+-30,+-60} x L_M 8개 x s{65,75,85,95} = 160케이스. 끝나면 재학습 →
    `_diag_s_breakdown.py`로 s=60-100mm MAE(현재 9.49mm)가 줄었는지 확인.
    **|phi|>=90 Fx_board 문제(20번)는 이미 그 구간을 s=15~95 전체 오프셋 격자로
    조밀화 완료한 상태라 같은 방식을 반복해도 효과가 낮을 수 있음 — 급하게 손대지
    말고 다음 세션에서 다른 접근("실패한 특정 조합만 재시도" 등)을 고민할 것.**

24. **[신규, 2026-09-07] s 공간가중치 + 실측 파인튜닝 2단계 — 코드 구현 완료, 실행 대기**:
    사용자가 domain adaptation/spatial-weighted loss/attention 관련 질문을 한 걸 계기로
    두 가지 새 레버를 `train_segment_classifier_singleprobe_beta0180_4seg.py`에 직접
    구현함(CPU 소규모 드라이런 N_SAMPLES=1000/N_EPOCHS=2로 실행 경로 검증 완료 - 버그 1개
    발견해서 수정함(파인튜닝 데이터에 probe 차원 누락으로 CNN 입력 shape 에러), 재실행해서
    끝까지 통과 확인. 단, 이 규모는 코드가 도는지만 확인한 것이라 성능 수치는 무의미함).
    - **s 공간가중치**(`s_spatial_weight()`): s=40~100mm 구간에 최대 3배 램프 가중치.
    - **실측 파인튜닝(2단계)**: 150k 합성데이터 학습(1단계) 후 실측 fit_rows(419개)로
      15epoch/lr=5e-5 추가 파인튜닝(2단계). `|phi|>=90` 실측 샘플 4배 오버샘플링.
      지금까지 실측 데이터가 CNN 가중치를 gradient로 직접 업데이트한 적이 한 번도
      없었다는 걸 이번에 처음 발견해서 나온 아이디어 - surrogate 병목을 안 거치는
      유일한 학습 경로라 HIGH_PHI_WEIGHT류 손실가중치와는 메커니즘이 다름.
    - 학습 로그에 "[1단계(파인튜닝 전)]" vs "[2단계(파인튜닝 후)]" 실측 홀드아웃 성능이
      나란히 찍혀서 직접 비교 가능. `FINETUNE_ON_REAL=0`/`S_WEIGHT_MAX=1.0` 환경변수로
      각각 끌 수 있음(원상복구용).
    - **실행**: 위 "지금 할 일" 24번 참고, GPU에서 정식 규모(150k/60epoch)로 재학습해서
      |phi|>=90 Fx_board R²(기준 0.355)/s>=60mm MAE(기준 9.49mm) 개선 여부 확인 필요.
      fit_rows가 419개뿐이라 과적합 위험 있음 - 실측 홀드아웃(진짜 판단 기준)과
      합성-val(참고용)이 서로 다른 방향으로 움직이면 과적합 신호로 볼 것.

---

## 📊 핵심 결과 요약

**⚠️ Fx/Fy 이름 주의 (숫자 읽기 전 필독)**: `force_model.py` 로컬좌표와 홀센서 보드좌표는
90도 회전 관계라 힘 벡터 축도 바뀜: **board_Fx = local_Fy, board_Fy = local_Fx**. 대체모델
자체 리포트는 로컬 이름(`Fx_total_N`,`Fy_total_N`)을, 최종 CNN 리포트는 보드 이름
(`Fx_board_N`,`Fy_board_N`)을 쓰는데 **이 둘은 서로 반대 데이터를 가리킴**(버그 아님, 좌표
변환상 정상 — 이 세션에서 이것 때문에 두 번 헷갈렸음). "Fx_board_N"의 진짜 정체는 로컬
Fy_total_N(대체모델 5-fold R²=-0.01, 원래 못 배우는 타겟), "Fy_board_N"의 진짜 정체는
로컬 Fx_total_N(대체모델 단독 R²=0.94로 잘 배우는 타겟).

| 모델/검증방식 | 구간분류 balanced acc | 연속값 s (R², MAE) | "Fx_board"(=로컬Fy, 원래 못 배움) | "Fy_board"(=로컬Fx, 원래 잘 배움) |
|---|---|---|---|---|
| multi-probe(11)+5구간 (2026-08-11, 옛 물리상수, 합성-val) | 86.0% | 0.954, 4.37mm | 0.90 | 0.88 |
| single-probe(1)+4구간+beta0180 (옛 물리상수, 합성-val) | 91.0% | 0.938, 3.06mm | 0.943 | 0.974 |
| ↑ 동일 스크립트, 새 물리상수, 결함 수정 후, FEA 244개 **합성-val** | 88.1% | 0.920, 3.54mm | 0.799 | 0.884 |
| ↑ 동일 모델(FEA 244개), **순수 실측 FEA 홀드아웃(n=36)** | 73.4% | 0.819, 7.41mm | 0.009 | 0.337 |
| ↑ FEA 402개(L_M 조밀화 후)로 재학습, **합성-val** | 88.1% | 0.933, 3.40mm | 0.811 | 0.909 |
| ↑ 동일 모델(FEA 402개), **순수 실측 FEA 홀드아웃(n=80)** — **✅ 힘 R² 대폭 개선** | 73.9% | 0.820, 7.96mm | 0.729 | 0.646 |
| ↑ FEA 518개(phi=90~150 s격자 조밀화 후)로 재학습, **합성-val** | 86.5% | 0.908, 3.78mm | 0.815 | 0.868 |
| ↑ 동일 모델(FEA 518개), 순수 실측 FEA 홀드아웃(n=103) — ⚠️ 아래 줄과 비교하지 말 것(홀드아웃 분할 버그, 폐기) | ~~75.8%~~ | ~~0.839, 6.09mm~~ | ~~0.462~~ | ~~0.750~~ |
| ↑ FEA 518개, 홀드아웃 분할 버그(전체 랜덤 20% → 데이터 늘어날 때마다 딴 행이 뽑힘) 수정 후 **동일 데이터로 재학습, 합성-val** | 88.0% | 0.924, 3.52mm | 0.846 | 0.891 |
| ↑ 동일 모델, **순수 실측 FEA 홀드아웃(n=99, 해시기반 고정분할)** — 여기서부터 진짜 비교 가능한 기준선 | 78.7% | 0.899, 5.73mm | 0.661 | 0.747 |
| ↑ config_head 실측검증 추가 + \|phi\|>=90 힘손실가중치 3배로 재학습, **합성-val** | 87.8% | 0.921, 3.76mm | 0.861 | 0.896 |
| ↑ 동일 모델, **순수 실측 FEA 홀드아웃(n=99)** — 엇갈린 결과(Fy_board/balanced acc 개선, Fx_board 하락) | 83.9% | 0.904, 5.91mm | 0.601 | 0.857 |
| ↑ L_M=0/12.5mm MOM 재확보(128개 교체) 후 재학습, **합성-val** | 88.2% | 0.926, 3.53mm | 0.849 | 0.916 |
| ↑ 동일 모델, 순수 실측 FEA 홀드아웃(n=99) — Fx_board 개선/Fy_board 하락(시드 미고정, 아래 줄에서 재확인됨) | 82.4% | 0.894, 5.80mm | 0.653 | 0.648 |
| ↑ 시드 고정(`torch.manual_seed(42)`) 추가 후 **동일 데이터로 재학습, 합성-val** | 87.4% | 0.922, 3.66mm | 0.824 | 0.904 |
| ↑ 동일 모델, 순수 실측 FEA 홀드아웃(n=99) — 시드 고정 이후 첫 결과 | 83.9% | 0.887, 6.13mm | 0.638 | 0.767 |
| ↑ L_M 2단계 구조(`lm_zero_head` 추가) 적용 후 **재학습, 합성-val** | 88.1% | 0.923, 3.62mm | 0.844 | 0.915 |
| ↑ 동일 모델, 순수 실측 FEA 홀드아웃(n=99) — L_M=0 사실상 해결됐지만 Fx_board 급락(시드=42, 아래에서 시드노이즈로 확정됨) | 80.8% | 0.885, 6.34mm | 0.145 | 0.871 |
| ↑ 진단용(커밋 안 함): 동일 코드, `FINAL_SEED=43` — Fx_board는 시드노이즈였음을 확인 | (시드42/43 각각 78.7/81.7% balanced acc) | - | **0.581**(시드43) | - |
| ↑ HIGH_PHI_WEIGHT 3.0→1.5로 재튜닝 후 **재학습(시드=42, 순수비교), 합성-val** | 87.9% | 0.923, 3.58mm | 0.831 | 0.912 |
| ↑ 동일 모델, **순수 실측 FEA 홀드아웃(n=99)** — ⚠️ 최신. \|phi\|>=90 Fx_board=0.355로 개선 없음(0.343과 사실상 동일) | 80.2% | 0.871, 6.49mm | **0.600** | 0.877 |

**L_M/phi(config_head) 실측 검증**: L_M_mm R²=0.565→0.669→0.714→**0.831**로 라운드마다
개선되던 중, 2026-08-27 `lm_zero_head`(L_M<6.25mm 이진분류) 추가로 **L_M=0 문제 사실상
해결**: `_diag_lm_by_value_breakdown.py`(하이브리드 비교 추가) 기준 L_M=0 MAE
19.36mm(순수 회귀)→**7.70mm(하이브리드)**, 나머지 구간(2.7~7.9mm)과 비슷한 수준이 됨.
phi_deg R²는 0.92~0.96대로 라운드 내내 안정적, 문제 없음. **같은 재학습에서 Fx_board
R²가 0.638→0.145로 떨어졌던 건 시드=43 진단 재학습으로 순수 시드 노이즈임이 확정됨**
(위 "지금 할 일" 21번 참고). 이후 HIGH_PHI_WEIGHT를 1.5로 낮춰 재시도했지만 목표
구간(\|phi\|>=90) Fx_board는 여전히 0.34~0.36대로 개선이 없어 이 레버는 폐기(20번 참고).

**왜 이게 필요한가 (힘 추정이 무너진 이유, 이미 규명 완료 → 2026-08-20 L_M 조밀화로 해결)**: 비판적 리뷰로 "서로게이트
환각 학습" 문제를 발견함 — 구간분류(88.1%→73.4%)도, 원래 잘 배우던 힘 타겟(대체모델 단독
R²=0.940→파이프라인 통과 후 0.337)도 실측 기준으로는 크게 떨어짐. 원인 추적:
1. 데이터 부족 문제가 아님 확인(대체모델 자체는 145개로도 힘을 R²=0.94로 잘 배움).
2. 진짜 원인: CNN 입력(B-field)이 대체모델이 예측한 **변위**(tip_ux,uy)로 만들어지는데, 이
   변위 예측이 약함(R²=0.52~0.68) → CNN이 진짜 물리가 아니라 "대체모델 특유의 변위→힘
   매핑을 B-field 인코딩 너머로 되돌리는 법"을 배움 (직접 검증: 대체모델 스타일 입력 주면
   대체모델 자신의 예측과 R²=0.896으로 거의 일치, 실측 변위로 만든 입력 주면 R²=0.337로 붕괴).
3. 데이터 없이 고치는 시도 2개 실패: 피처 엔지니어링(효과 없음), `force_model.py` 정밀
   물리모델로 변위 대체(오히려 더 나쁨, R² 음수 — 선형 보 이론이 비선형 접촉변형을 못 담음).
4. **결론**: 실측 FEA를 늘려서 변위 예측 정확도를 올리는 것만이 남은 해법이었음 → **2026-08-20
   L_M 조밀화(158개 추가)로 실행, 가설 확인됨**: 힘 R²(실측 홀드아웃) 0.009/0.337 →
   **0.729/0.646**로 대폭 개선. 완전히 안 좋아진 목표(0.9+)는 아직 아니지만 "사실상 못 씀"에서
   "쓸만함"으로 넘어옴.
5. **2026-08-21 phi=90~150 s격자 조밀화(114개 추가) 후 "Fx_board 0.729→0.462 악화"로 보였던
   건 측정 버그였음, 2026-08-25 확정**: 홀드아웃을 뽑던 코드가 `rng(42).permutation(전체
   개수)`로 "전체의 랜덤 20%"를 뽑는 방식이라, 데이터 개수가 바뀌면(404→518) 같은 시드를
   써도 뽑히는 행 자체가 완전히 달라짐 — 지난 홀드아웃(n=80)과 이번(n=103)이 애초에 서로
   다른 검증셋이라 직접 비교가 안 맞았음. phi별로 쪼개보니 |phi|<90 구간은 R²=0.64로 여전히
   정상, |phi|>=90 구간(원래도 낮았음)은 R²=0.39로 원래부터 낮았던 것 — 고각도 데이터 비중이
   43%→56%로 늘면서 랜덤 홀드아웃에 어려운 구간이 더 많이 뽑혀 평균이 끌려 내려간 것뿐,
   모델이나 데이터가 나빠진 게 아니었음. **수정**: 홀드아웃을 행 고유키(L_M,phi,beta,s)
   해시 기반 고정 분할로 교체(데이터가 늘어도 기존 행의 소속이 절대 안 바뀜) → 재학습 →
   **새 진짜 기준선: 구간분류 78.7%, s R²=0.899, Fx_board R²=0.661, Fy_board R²=0.747**.
   이 숫자부터 앞으로 계속 비교할 것.
6. **2026-08-25(2차) config_head 실측검증 + 고각도 힘손실가중치 3배 실험, 둘 다 결과 엇갈림**:
   L_M 실측 R²=0.565가 합성-val(0.895)보다 크게 낮음(phi는 문제 없음, 0.928) — 원인 미조사,
   위 "지금 할 일" 10번 참고. 힘손실가중치는 balanced acc(78.7%→83.9%)/Fy_board
   (0.747→0.857)는 개선했지만 **Fx_board는 0.661→0.601로 하락**, 특히 |phi|>=90 구간
   R²=0.343로 여전히 약함 — 가중치만으론 해결 안 됨, 위 "지금 할 일" 10번 참고.
7. **2026-08-26 L_M=0 MOM 재확보 시도, 가설 반박됨**: "frac 근사가 신호를 지웠다"는 가설로
   MOM 실측 변위를 FEA에서 직접 뽑도록 고쳐서 128개 재확보 후 재학습했지만, L_M=0만 따로
   보면 MAE가 오히려 40mm→46.52mm로 나빠짐(L_M≠0은 R²=0.932로 계속 좋음). 대체모델 자체는
   mom_ux/uy를 R²=0.769/0.872로 잘 배우는데도 최종 B-field 기반 예측은 안 됨 — 근사식
   문제가 아니라 **L_M=0 근처에서 B-field 자체가 L_M을 구분할 정보를 안 담고 있을 가능성**
   으로 가설 전환, 위 "지금 할 일" 11번(magpylib 직접 확인) 참고.
8. **2026-08-26 magpylib 확인 + 시드 고정 재학습, "물리적 한계" 가설도 반박됨**: 11번에서
   B-field 구분신호가 L_M=0~12.5mm 구간도 다른 구간과 비슷함을 확인(하드웨어 한계 가설
   기각). 최종 CNN 학습에 시드 고정이 없었던 버그도 발견/수정 후 재학습(13번) — Fy_board는
   0.648→0.767로 회복(이전 하락 상당 부분이 시드 노이즈였음을 뒷받침)했지만, **L_M=0
   MAE=26.3mm는 시드를 바꿔도 재현**(나머지 구간 2.9~6.5mm 대비 4~9배 나쁨) — 신호도
   있고 대체모델도 배우는데 최종 CNN만 재현성 있게 실패, 원인 미확정. 다음 후보는 위
   "지금 할 일" 14번(다변량 식별가능성 테스트) 또는 구조적 특이점(L_M=0=K1 길이 0) 가능성.
9. **2026-08-27 L_M 2단계 구조(lm_zero_head)로 L_M=0 사실상 해결, Fx_board 급락은
   시드 노이즈로 확정, HIGH_PHI_WEIGHT 레버는 폐기**: 14번에서 L_M=0 vs 12.5mm 이진분류가
   99.3%로 오히려 잘 되는 구간보다도 쉽다는 걸 확인("분류는 완벽, 연속회귀만 실패") →
   `lm_zero_head`(L_M<6.25mm 이진분류)를 추가해 분류 결과가 "0에 가깝다"면 0mm를,
   아니면 기존 회귀값을 쓰는 하이브리드 구조로 재학습 → **L_M=0 MAE 19.36mm→7.70mm로
   다른 구간과 비슷한 수준까지 개선, 목표 달성**(단, 시드별 편차 있음 - 21번 참고).
   같은 재학습에서 Fx_board R²가 0.638→0.145로 떨어졌던 건 시드=43 진단으로 **순수
   시드 노이즈임이 확정**됨. 이후 HIGH_PHI_WEIGHT를 3.0→1.5로 낮춰 재시도했지만
   목표(|phi|>=90 Fx_board)는 0.343→0.355로 개선이 없어 **이 레버는 폐기, 실측 FEA
   추가로 넘어갈 것**(20번 참고).

**모델 파일**: `models/position_segment_classifier_singleprobe_beta0180_4seg.pth` (2026-09-11
25번 반영 최신 재학습본 — L_M/phi가 예측 대상이 아니라 known input으로 바뀜, 시드43+
파인튜닝없음 채택. balanced acc 83.0%, s R²=0.910, Fx_board(전체) R²=0.626, |phi|>=90
Fx_board R²=0.440, Fy_board R²=0.855 — 위 25번 표 참고. **체크포인트 포맷이 바뀌어서
`config_head`/`lm_zero_head` 없음 - 옛 5-output 구조를 가정하는 진단 스크립트는 이 파일을
못 읽음(25번 다음 세션 항목 참고).** L_M=0 문제는 L_M 자체를 더 이상 추정하지 않으므로
자연히 해소됨. 학습 스크립트에는 그 이전(2026-08-19) 비판적 리뷰로 찾은 결함 5개 수정도
계속 반영돼 있음(Fy 가중치 축소, 실측 홀드아웃 분리, 형상 그룹분리 train/val, balanced
acc 기준 체크포인트, 서로게이트 불일치 리젝션 샘플링).
진단 스크립트(전부 `현서/scripts/contact_scenarios/fea/`): `_diag_surrogate_only.py`(대체모델
단독 홀드아웃 검증), `_diag_pipeline_leakage.py`(환각학습 직접 증명), `_diag_failure_by_phi.py`
(FEA 실패율 phi별 집계).

---

## ⚙️ 물리 상수 현황

| 상수 | 값 | 상태 |
|---|---|---|
| BR (자석 잔류자속밀도) | **0.4T** | 확정 (교수님 MATLAB 코드 값) |
| K1 (베이스~MOM 굽힘강성) | **1.2517e-6 N·m²** | 확정 |
| K2 (MOM~팁 굽힘강성) | **6.2586e-7 N·m²** | 확정, `material_convert.py`도 동일 E 공유 |
| 구조 | **3구간 (K1/K_RIGID/K2)** | ✅ 확정 — 논문 원문(식 9 아래)에서 MOM 8mm를 안 휘는 직선구간으로 명시 확인함. **2구간으로 바꾸지 말 것** (2026-08-20 오전에 실수로 바꿨다가 논문 재확인 후 원복함) |
| M1 (MOM자석 길이, 자기모멘트용) | **8mm** | 확정 — 사용자가 실제 하드웨어 스펙이라고 직접 확인함 |
| EN_FACTOR (K1=EN×K2) | 2.0 | ⚠️ 근거 불명, 급하지 않음. 와이어 보강 때문은 아님(아래). 논문도 K1이 부정확함을 인정(Section IV-A) |

`force_model.py`는 `scripts/force_model/`과 `현서/scripts/force_model/` 두 곳에 동일 사본이
있음 — **고칠 때 항상 둘 다 동기화할 것** (`diff --strip-trailing-cr` 로 확인, CRLF/LF 차이는
무시). 논문 PDF: `현서/data/contact_scenarios/fea/*.pdf`는 손상됨(텍스트/이미지 추출 둘 다
안 됨) — `C:\Users\gustj\Desktop\김현서\김현서\Workspace_Expansion_...pdf`(정상 사본) 사용할 것.

---

## 🔑 그 외 확정된 사실들 (참고용, 재작업 불필요)

- **MOM 변위를 "팁 변위의 L_M/100"으로 근사하던 frac 휴리스틱, 근본 원인이었음(2026-08-26
  확정)**: FEA가 원래 팁(N_TIP) 변위만 뽑고 MOM 위치의 실제 변위는 안 뽑아서, 학습
  데이터/실측 검증 양쪽 다 `frac=L_M/100`으로 근사해왔음. L_M=0(MOM이 베이스 바로 옆)에서
  frac=0이 되어 MOM이 접촉에도 전혀 안 움직이는 걸로 계산됨 → 그 지점의 B-field 신호에
  L_M 정보가 거의 안 실림 → L_M 실측 R²=0.565(L_M≠0만 따로 보면 R²=0.953으로 정상,
  L_M=0만 MAE=40mm로 랜덤 수준). 직접 계산 검증: L_M=0→2.5mm 구간 신호 변화량 0.63 vs
  L_M=40→60mm 구간 11.7(20배 차이). **수정**: `make_bent_contact_scene.py`가 MOM
  강체구간(N_MOM, s=a1~a2) 절점을 지오메트리 분할 없이(자코비안 문제 회피) 사후
  최근접-중심선-매칭으로 골라내고, `run_contact.py`가 그 실제 변위(mom_ux/uy/uz_avg_mm)와
  회전(mom_theta_deg)을 출력하도록 확장. 대체모델 TARGETS에 mom_* 추가, frac 계산 완전
  제거(하위호환용 fallback은 옛 FEA 로드 시점에만 적용). **기존 FEA는 재사용 불가(원본
  결과파일이 정리 과정에서 삭제됨 + N_MOM 자체가 옛 메시엔 없음)** - L_M=0/12.5mm부터
  우선 재확보 중(위 "지금 할 일" 10번).
- **해석모델(force_model.py)의 다중해(가지 점프) 위치, 정확히 특정함**: `solve_shape`를
  phi=0부터 연속법으로 추적했을 때 L_M/L=0, 0.25는 -150°~150° 전 구간 매끈함(점프 없음).
  L_M/L=0.5는 **phi=±95°→±100° 사이**에서만(Δθ≈20°), L_M/L=0.75는 **phi=±145°→±150°
  사이**에서만(Δθ≈45°) 점프함 - 나머지는 전부 매끈함. 다만 **FEA 접촉 수렴 실패는 이거랑
  별개 원인**이라(L_M 무관하게 phi≥90에서 전반적으로 낮음, 위 표 참고) L_M이 작다고 FEA가
  잘 풀리는 건 아님 - 두 문제를 헷갈리지 말 것.
  - **L_M 조밀화 신규값(12.5/37.5/62.5/87.5mm)도 같은 방식으로 확인 완료(2026-08-20)**:
    L_M=12.5mm, 37.5mm는 -150°~150° 전 구간(연속법 기준) 매끈함. L_M=62.5mm는
    phi=130°→135° 사이 점프(Δθ≈-29.8°), L_M=87.5mm는 phi=100°→105° 사이 점프(Δθ≈-70.5°).
    **다만 실제 스윕 스크립트(`get_bent_centerline.py`)는 연속법이 아니라 힌트 없는
    `solve_shape()`(매 phi 독립 전역 탐색)를 씀** — 그래서 실제 스윕이 쓰는 이산 격자점
    {0,±30,±60,±90,±120,±150}만으로 직접 재확인한 결과: **L_M=62.5mm는 phi=±150°에서만
    틀림**(θ_L 순서 0→31.6→63.3→95.3→128.1→**125.09(150°, 감소 - 오답)**), **L_M=87.5mm는
    phi=±120°, ±150° 둘 다 틀림**(θ_L 순서 0→34.5→69.5→106.1→**62.97(120°, 급감 - 오답)**
    →**90.40(150°, 역시 오답)**). L_M=12.5mm, 37.5mm는 이 격자점 방식으로도 전부 매끈함
    (재확인 완료). 이 6조합(총 60케이스)은 `run_lm_densify_matv2_sweep.sh`에서 이미 제외됨.
- **니티놀 와이어는 접촉힘에 기여 안 함, 확정**: 실측 스펙(지름100um, E=28GPa)으로 K1 구간에
  임베드해서 FEA 돌려봤는데 접촉힘 변화 거의 0(-0.07%±0.29%). 논문에 "윤활을 위해 30cs
  실리콘 오일을 튜브 안에 넣는다"는 내용이 있어서(Section II-B) 와이어가 벽에 접착된 게
  아니라 오일 속에서 미끄러지는 부품이라는 물리적 설명과 일치 — `include_wire=False`(기본값)
  그대로 두면 됨.
- **beta=0/180 대칭성 확인됨**: F_mag 상대오차 중앙값 1.66% — beta=180 데이터도 이미 48개
  실측으로 있어서 부호반전 증강 불필요(실제로 tip_uz는 단순 부호반전이 아니라 증강했으면
  버그 생길 뻔함).
- **`train_segment_classifier_singleprobe_beta0180_4seg.py`는 실재하는 의도된 최종
  스크립트**(오타나 유령 파일 아님) — single-probe+4구간+beta0180 전부 의도적 사양.
  `train_segment_classifier_multiprobe_auxreg.py`(multi-probe+5구간, 86.0%)는 이것보다 이전
  단계 결과물.
- **FEA phi별 실패율** (`_diag_failure_by_phi.py`, beta=0/360케이스 기준, STABILIZE 적용 전):
  phi=0: 15%, ±30: 62~72%, ±90: 80~82%, ±120: 72~75%, ±150: 45~62%. STABILIZE 적용 후
  phi=±60(신규): 40% 성공(가장 좋음), phi=90~150은 여전히 대부분 실패.
- **초기 FEA 664개 vs 새 물리상수 불일치 문제**: 2026-08-18에 K1/K2/BR을 논문 디지타이징
  피팅값에서 교수님 MATLAB 방식(E·I 직접계산)으로 바꾸면서, 옛 664개 FEA가 새 상수와 안
  맞게 됨 → beta=0/180만 최소 규모(450케이스 시도)로 재생성하는 것으로 결정, 진행 중.
  `material_convert.py`도 같은 날 E값 공유하도록 동기화함.

---

## 📁 프로젝트 구조 / 환경

- `scripts/force_model/` — 물리모델 (K1,K2,M1 등), `현서/scripts/force_model/`에 동기화 사본
- `현서/scripts/cnn_model/` — 홀센서→자석 위치추정 CNN (예전 성과, 잘 됨)
- `현서/scripts/contact_scenarios/` — 접촉(충돌) 감지 연구
- `현서/scripts/contact_scenarios/fea/` — CalculiX+Gmsh FEA (주 작업 위치)

```powershell
pip install -r requirements.txt
conda install -c conda-forge calculix
```
GPU 있으면 15만개 합성+CNN 학습 전체 ~25~110분(GPU 사양에 따라 다름, RTX 5080에서 24.3분
전례 있음). 이 컴퓨터(문서 정리한 컴퓨터)는 CPU-only라 오래 걸림 — 가능하면 GPU 컴퓨터에서
재학습 실행 권장.

**⚠️ Windows+conda에서 `ccx`(CalculiX) PATH 문제 (2026-09-14 발견)**: conda로 설치하면
`ccx.exe`가 `<conda경로>\Library\bin\`에 생기는데, 이게 기본 PATH에 없어서 `run_contact.py`의
`subprocess.run(["ccx", ...])`가 "파일을 찾을 수 없음"으로 실패함. FEA 스크립트 실행 전에
PowerShell에서:
```powershell
$env:PATH = "<conda경로>;<conda경로>\Library\mingw-w64\bin;<conda경로>\Library\usr\bin;<conda경로>\Library\bin;<conda경로>\Scripts;$env:PATH"
```
(mingw-w64\bin에 ccx가 필요로 하는 런타임 DLL이 있어서 Library\bin만으론 부족함 -
DLL_NOT_FOUND로 죽었었음). 추가로 `run_contact.py`/`sweep_lm_phi_position_matv2_worker.py`의
`subprocess.run`에 `encoding="utf-8", errors="replace"`를 명시해둠 - 한글 로캘(cp949)
Windows에서 ccx 출력에 cp949로 못 읽는 바이트가 섞여 있으면 UnicodeDecodeError로 죽던
버그였음(2026-09-14 수정 완료, 커밋됨).

---

## 🗄️ 이전 세션(2026-08-05~11) 최종 결과 — 지금은 옛 물리상수 기준, 참고용

당시 최종 스크립트: `sweep_lm_phi_position_worker.py`(FEA 608개, L_M4×phi11×s10) →
`train_segment_classifier_multiprobe_auxreg.py`(11프로브 능동탐색, 구간분류+s+Fx,Fy 멀티태스크).
결과: balanced acc 86.0%(무작위 20%), s R²=0.954, Fx/Fy/F_mag R²=0.90/0.88/0.92.

**원인 규명 과정에서 나온, 지금도 유효한 교훈들**:
- **합성 B-field에 인위적 센서 노이즈(5%)를 넣었더니 오히려 정확도가 28.7%→81.6%로
  폭락했었음** — 실제 하드웨어는 FFT로 노이즈를 거르므로, 노이즈 없이 만드는 게 더 정확한
  시뮬레이션. **앞으로도 합성 B-field 데이터에 인위적 노이즈 넣지 말 것.**
- 대체모델이 F_mag을 직접 예측하면 음수가 나올 수 있음(물리적 제약 없음) → Fx,Fy만 예측하고
  F_mag=sqrt(Fx²+Fy²)로 유도하는 방식이 항상 더 안전함(지금 파이프라인도 이 방식 유지).
- 오답의 98%+ 가 "바로 옆 구간"과의 혼동이라, 연속값(s) 보조회귀를 같이 학습시키면 경계
  부근 표현력이 보강되어 분류 정확도도 같이 오름.
- push_depth/beta 고정, 더 큰 네트워크로 재학습 — 전부 효과 없었거나 악화(재시도 불필요).
- FEA는 s(팁에 가까울수록)가 클수록 원래 수렴 실패율이 높음(지렛대 효과로 신호가 약해서,
  버그 아님).

**아직 안 한 것**: 하드웨어 실측 검증(지금까지 전부 시뮬레이션 기준), "FFT로 노이즈 필터링"
가정의 실측 확인.
