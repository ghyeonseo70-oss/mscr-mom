# 프로젝트 현황 (인수인계, 2026-08-19 최종 업데이트)

이 문서는 다른 컴퓨터/세션에서 새로 시작하는 Claude에게 상황을 알려주기 위한 것입니다.
**새 세션에서 클로드에게 할 말**: "PROJECT_STATUS.md 읽고 여기서부터 이어서 진행해줘"

## 📊 지금까지 결과값 정리 (한눈에 보기)

**분류/회귀 모델 성능** (검증 15,000개 기준, 각 행이 어떻게 검증됐는지 꼭 구분해서 볼 것):

| 모델/검증방식 | 구간분류 balanced acc | 연속값 s (R², MAE) | Fx (R²) | Fy (R²) |
|---|---|---|---|---|
| multi-probe(11)+5구간 (2026-08-11, 옛 상수, 합성-val) | 86.0% | 0.954, 4.37mm | 0.90 | 0.88 |
| single-probe(1)+4구간+beta0180 (옛 상수, 합성-val) | 91.0% | 0.938, 3.06mm | 0.943 | 0.974 |
| ↑ 동일 스크립트, 새 상수, **합성-val**(1차 재학습, 순환검증 결함 있었음) | 91.8% | 0.961, 2.71mm | 0.948 | 0.936 |
| ↑ 동일 스크립트, 새 상수, 결함 5개 수정 후, **합성-val**(형상 그룹분리) | 88.1% | 0.920, 3.54mm | 0.799 | 0.884 |
| ↑ 동일 모델, **순수 실측 FEA 홀드아웃**(n=36, 대체모델 안 거침) — **⚠️ 가장 신뢰할 수 있는 숫자** | **73.4%** | **0.819, 7.41mm** | **0.009** | **0.337** |

**⚠️ 2026-08-19 최종 결론 (이 세션에서 확정)**: 비판적 리뷰가 우려했던 "서로게이트 환각 학습"이
**실제로 있었음** — 합성-val 88.1%(구간분류) vs 실측 홀드아웃 73.4%로 **14.7%p 차이**(경보
기준 10%p를 넘음), Fx는 합성-val 0.799인데 실측에서는 **R²=0.009로 사실상 예측력 없음**.
즉 지금까지 보고된 86~92%는 전부 "대체모델이 만든 세계 안에서의 자기검증" 수치이고, **실제
FEA(실측)에 대한 진짜 일반화 성능은 구간분류 기준 약 73%, 힘(Fx/Fy) 추정은 거의 못 씀**이
현재까지 가장 정직한 결론임. 다만 n=36이라 신뢰구간이 넓어서(옛 664개 스윕이 아니라 새
181개 기준이라 홀드아웃 표본 자체가 작음) 정확한 참값은 불확실 - 구간분류 73%가 무작위(25%)
보다는 확실히 잘 하지만, 힘 추정은 재검토가 필요해 보임. 모델:
`models/position_segment_classifier_singleprobe_beta0180_4seg.pth` (마지막 실행 결과로 덮어씀).
재학습 로그: `run_singleprobe_beta0180_4seg_hardened.log`. GPU(RTX 5080)로 24.5분 소요.

**다음에 할 일 후보** (급한 순서 아님, 사용자 판단 필요):
1. 실측 FEA를 더 늘려서(지금 181개는 홀드아웃 36개 빼면 대체모델 학습에 145개뿐) 대체모델
   자체의 신뢰도를 높이는 게 근본 해법 - 특히 Fy/Fz/tip_uz는 대체모델 5-fold R²도 음수라
   대체모델부터 못 미더움.
2. 힘(Fx/Fy) 추정을 포기하고 구간분류+s 위치만 목표로 재설계하는 것도 고려 가능 (힘 쪽
   실측 R²가 거의 0이라 지금 상태로는 실용적 가치가 없어보임).
3. 논문/교수님께 "힘 추정은 지금 데이터로는 신뢰 못 함, 구간분류는 실측 기준 약 73%"로
   정직하게 보고할 것 - 91%대 숫자를 그대로 보고하면 안 됨.

**물리 상수** (2026-08-18에 논문 디지타이징 피팅 → 교수님 MATLAB 코드(`code (2).txt`) 방식으로 전환):

| 상수 | 옛 값(논문 피팅) | 새 값(MATLAB E·I) | 비고 |
|---|---|---|---|
| BR (자석 잔류자속밀도) | 0.36T | **0.4T** | 확정, 두 파일 동기화됨 |
| K1 (베이스~MOM 굽힘강성) | 3.82e-6 N·m² | **1.2517e-6 N·m²** | 확정 |
| K2 (MOM~팁 굽힘강성) | 9.89e-7 N·m² | **6.2586e-7 N·m²** | 확정, `material_convert.py`도 동기화 완료 |
| M1 (MOM자석 자기모멘트 계산용 길이) | (배율 1.29 적용) | 8mm 사용 중 | ⚠️ **미해결**: MATLAB 실제 계산은 6mm 나옴(코드 주석은 "4mm"라 써서 더 헷갈림), 8mm는 아마 하우징+와이어 연결부 포함 길이라 M1(자석 재질만)엔 6mm가 맞을 가능성 있음 — 사용자가 "일단 두라"고 해서 미수정 |
| EN_FACTOR (K1=EN×K2) | - | 2.0 (가정값) | ⚠️ 와이어 FEA 실험에서 이 가정에 의문 제기됨(아래) |

**FEA 재스윕(beta=0/180 전용, 새 물리상수 반영)**:
- 재료값 검증 스윕: **181/450 성공(40%)** — beta=0 133개, beta=180 48개. 실패율은 옛 664개
  스윕과 같은 패턴(phi 극단값 근처 CalculiX 수렴 실패)이라 새 상수 때문이 아님.
- beta=0/180 대칭성 검증: **통과**(F_mag 상대오차 중앙값 1.66%) — 이론상 필요 없어졌지만
  실측 48개가 이미 있어서 안 써도 됨(아래 참고).
- 니티놀 와이어 FEA 실험(K1=2×K2 가정 검증 시도): 사용자 실측 스펙(지름100um, E=28GPa)으로
  와이어를 K1 구간에 임베드해서 돌려봤는데 **접촉힘에 거의 영향 없음**(-0.07%±0.29%, 사실상
  0) — **원인 규명됨**: 논문상 와이어는 윤활유 채워진 내강 속 슬라이딩 텐던(접착 안 됨)이라
  물리적으로도 맞는 결과, FEA도 우연히 완전분리로 풀려서 결론은 유효(아래 5번 마지막 문단).
  EN_FACTOR=2.0의 근거는 와이어 보강이 아닌 다른 이유일 가능성(미확인, 급하지 않음).

**재학습**: ✅ 1차 완료(91.8%, 순환검증 결함 있었음) → ✅ **결함 5개 수정 후 재실행도 완료
(실측 홀드아웃 기준 73.4%로 하향 확정)** — 위 표/결론 참고. 재학습 자체는 더 할 일 없음,
다음은 데이터/설계 문제(위 "다음에 할 일 후보").

### [2026-08-19] 비판적 리뷰로 찾은 파이프라인 결함 5가지 — 수정 완료, 재실행도 완료
`train_segment_classifier_singleprobe_beta0180_4seg.py`를 "치명적 결함을 찾아라" 식으로
비판적으로 재검토해서 실제 문제 5개를 찾아 코드로 고쳤음(스모크테스트 2000개/5에폭으로 5개
전부 정상 작동 확인, 전체 15만개/60에폭 실행은 안 함 - 여기서 할 일):

1. **Fy_total_N 학습 가중치 1/10로 축소**: 대체모델 5-fold R²가 Fy_total_N=-0.01(평균보다도
   못 맞춤, 사실상 노이즈)인데 이걸 그대로 정답으로 학습시키고 있었음. `force_head`의 두
   출력 중 어느 열이 진짜 Fy_total_N인지 헷갈리기 쉬우니 주의: `fb`/`fxy_all`은
   `[Fy_total_N(로컬), Fx_total_N(로컬)]` 순서(보드좌표 변환 때문에 "축교환"됨)이고,
   `force_names=["Fx_board_N","Fy_board_N"]`은 출력 **표시 라벨**일 뿐 순서를 안 바꿈 -
   즉 0번 열(표시상 "Fx_board_N")이 진짜 나쁜 타겟임. `FORCE_LOSS_WEIGHTS=[0.1, 1.0]`로 반영.
2. **실측 FEA 20%(36개) 홀드아웃 분리**: 181개 중 36개를 대체모델 학습에서 아예 빼고, 학습
   끝에 이 36개로 서로게이트를 안 거친 순수 실측 평가를 별도로 출력(새 섹션 "순수 실측 FEA
   검증"). 지금까지의 91.8%는 이게 없어서 "서로게이트가 만든 세계 안에서의 자기검증"이었음.
3. **train/val을 (L_M,phi) 형상 단위로 그룹 분리**(`GroupShuffleSplit`, 10mm/10deg 격자):
   무작위 90/10이면 `free_cache`로 재사용되는 거의 동일한 형상이 train/val에 걸쳐서 val
   정확도가 "안 본 형상 일반화력"이 아니라 "본 형상 보간력"을 재고 있었을 가능성.
4. **체크포인트 선정을 balanced accuracy 기준으로 변경**: 기존엔 스케일이 다른 4개 손실
   단순합(`val_loss`)이 최소인 epoch을 골랐는데, 이게 실제 목표(구간분류)에 최적이란 보장이
   없었음.
5. **서로게이트 앙상블 불일치 기반 rejection sampling**: 10개 앙상블이 서로 크게 갈리는(=
   실측 FEA가 부족해서 외삽 중인) `(L_M,phi,s,beta)` 샘플을 합성데이터 생성 중 걸러냄
   (`DISAGREEMENT_THRESHOLD` 환경변수, 기본 0.5).

**실행 결과 (2026-08-19, CalculiX/GPU 컴퓨터)**:
```bash
N_SAMPLES=150000 N_EPOCHS=60 python train_segment_classifier_singleprobe_beta0180_4seg.py
```
→ 합성-val balanced acc 88.1% vs **실측 홀드아웃(n=36) balanced acc 73.4%** — 14.7%p 차이로
경보 기준(10%p)을 넘음. **서로게이트 환각 학습 문제가 실제로 있었다는 게 확인됨.** 힘(Fx)은
합성-val 0.799인데 실측 R²=0.009로 사실상 붕괴 - 지금 힘 추정은 못 믿을 수준. 자세한 숫자는
문서 맨 위 표 참고.

## ⚠️ 2026-08-18~19 세션 업데이트 (CalculiX 있는 컴퓨터) — 스윕 + 재학습 전부 완료

이전 세션(다른 컴퓨터)에서 교수님이 주신 MATLAB 코드(`현서/scripts/contact_scenarios/code (2).txt`)를
분석하다가 물리 상수를 바꿨고, 그 결과 **기존 FEA 664개가 새 상수와 안 맞게 됨**. 이걸 최소
규모로 바로잡는 게 이번 세션의 할 일이었음(완료, 아래 3번).

**2026-08-19 업데이트**: 재료값-검증 스윕(181/450)과 와이어 스윕(143/360) 둘 다 완료, GitHub에
푸시됨. 와이어 효과가 거의 0%로 나온 원인은 규명됨(아래 5번 마지막 문단 참고) — 물리적으로도
"접착 안 됨"이 맞고(논문: 윤활유 채워진 내강, 와이어=슬라이딩 텐던), FEA 구현도 마침 완전
분리로 풀려서 결론 자체(접촉힘에 영향 없음)는 유효함, 다만 완전분리는 "단방향 접촉"보다 한 단계
더 느슨한 근사라 정밀화 여지는 남음. 누락됐던 재학습 스크립트(`..._singleprobe_beta0180_4seg.py`)
push 완료, SOURCES도 새 FEA 데이터로 교체 완료 — 재학습 실행만 남음(아래 5번).

### 1) 무엇이 바뀌었나
- `scripts/force_model/force_model.py`: `BR`(자석 잔류자속밀도) `0.36T → 0.4T`, K1/K2(굽힘강성)를
  논문 그림 디지타이징 피팅 대신 **교수님 MATLAB 코드 방식**(영률 E=850kPa 직접 대입,
  K2=E·I, K1=2·K2)으로 교체. 새 값: K1=1.2517e-06, K2=6.2586e-07 N·m² (옛 값 K1≈2.25~3.82e-6,
  K2≈8.53~9.89e-7 대비 더 유연함).
  **⚠️ 2026-08-18 추가 발견/수정**: 이 파일이 실제로는 두 벌 있었음 —
  git이 추적하는 `scripts/force_model/force_model.py`(루트, 이번 세션에 새 값으로 고침)와
  git이 추적 안 하는 낡은 사본 `현서/scripts/force_model/force_model.py`(폴더 이동 반영 안
  된 물리 파일, 옛 값 그대로). `get_bent_centerline.py` 등 `현서/` 밑 스크립트는 전부
  상대경로로 **후자(안 고쳐진 낡은 사본)**를 import하고 있어서, 스윕을 처음 돌렸을 때
  "형상=옛 뻣뻣한 K, 접촉재료=새 유연한 E"라는 또 다른 방향의 불일치로 45분간(30/450케이스)
  돌아갔었음 — 발견 즉시 중단하고 두 사본을 동기화(낡은 사본에 동일 수정 적용, diff 0으로
  확인)한 뒤 처음부터 재시작함. **앞으로 `force_model.py`를 고칠 땐 두 경로 모두 확인할 것**
  (`diff scripts/force_model/force_model.py 현서/scripts/force_model/force_model.py`로 동기화
  여부 항상 체크 권장 — 근본적으로는 이 두 사본을 하나로 정리하는 게 맞지만 이번 세션
  범위 밖이라 보류).
- `현서/scripts/contact_scenarios/fea/material_convert.py`: 이 파일이 FEA의 Neo-Hookean
  재료값(C10, D1)을 만드는 곳인데, **옛 K2(9.8872e-07)를 하드코딩**하고 있어서 force_model.py의
  새 K2와 어긋나 있었음(자유단 형상은 새 K2=더 유연, 접촉힘 계산은 옛 K2=더 뻣뻣한 재료 —
  물리적으로 모순). → **오늘 수정 완료**: 이제 force_model.py와 똑같은 `E=850kPa`를 직접 쓰도록
  고쳐서(K2를 거꾸로 역산하지 않음) 두 파일이 항상 같은 재료값을 공유하게 만듦. `run_contact.py`
  등이 `material_convert.py`를 매 실행시 새로 import하므로 **이 수정만으로 앞으로 돌리는 모든
  FEA는 자동으로 새 재료값을 씀** — 별도 조치 불필요.
- 문제: **기존 FEA 664개**(`fea_lm_phi_pos_sweep_all.json` 등 4개 파일, 대체모델/최종모델의
  학습 소스)는 전부 옛 재료값으로 이미 계산이 끝난 상태라 재사용 불가. 전체 664개를 다시 돌리는
  건 과함("600개는 에바" — 사용자 판단) — **지금 모델이 실제로 쓰는 건 beta=0°/180°뿐**이므로
  그것만 최소 규모로 다시 돈다.

### 2) 새로 만든 스윕 (beta=0/180 전용, 총 450케이스 — 옛 664개 대비 -32%)
`현서/scripts/contact_scenarios/fea/`에 3개 파일 추가:
- `sweep_lm_phi_position_matv2_worker.py` — 기존 `sweep_lm_phi_position_worker.py`와 동일한
  구조(조합 하나당 s 10곳), beta를 인자로 받도록만 바꿈. `material_convert.py`가 이미 새 E를
  쓰므로 이 워커는 그대로 실행하면 자동으로 새 재료값 사용.
- `run_lm_phi_position_matv2_sweep.sh` — 실행 스크립트. **beta=0**: L_M(4: 0,25,50,75mm) ×
  phi(9: 0,±30,±90,±120,±150°) × s(10) = 360케이스 (옛 스윕과 동일 격자밀도 그대로 — 이
  밀도가 91% 성능을 낸 격자라 더 줄이면 위험). **beta=180**: L_M(3: 0,50,75) × phi(3: 0,±90) ×
  s(10) = 90케이스만 — 전수 대신 "beta=0의 부호반전 = beta=180"이라는, 옛 재료값 데이터
  (`fea_beta_fine_resolution.json`)에서 이미 확인된 대칭성이 새 재료값에서도 유지되는지
  확인할 표본만.
  ```bash
  cd 현서/scripts/contact_scenarios/fea
  bash run_lm_phi_position_matv2_sweep.sh   # 결과: ../../../data/contact_scenarios/fea/fea_lm_phi_pos_matv2_all.json
  ```
- `check_beta180_symmetry.py` — 스윕 끝나면 실행. beta=180 90개 vs 대응 beta=0 케이스의
  부호반전 비교, F_mag 상대오차 중앙값 5% 이내면 "대칭성 유지 → beta=180 추가 스윕 불필요
  (beta=0 데이터를 부호반전해서 그대로 씀)", 아니면 "beta=180도 360개 전부 필요"라고 판정해줌
  (스크립트 안에 전체 스윕으로 확장하는 방법 설명 있음).
  ```bash
  python check_beta180_symmetry.py
  ```

### 3) 스윕 결과 (2026-08-18 완료)
- `run_lm_phi_position_matv2_sweep.sh` 완료: **181/450 성공(40.2%)** →
  `fea_lm_phi_pos_matv2_all.json`. 실패율(약 60%)은 예전 664개 스윕 때도 phi 극단값 근처에서
  비슷하게 높았던 것과 같은 종류(급한 굽힘에서 CalculiX 자코비안 실패) - 새 재료값 때문에
  악화된 게 아님(직접 대조 확인함, 아래 와이어 섹션 참고).
- `check_beta180_symmetry.py` 판정: **대칭성 유지** (F_mag 상대오차 중앙값 1.66%, 33표본,
  5% 기준 이내) → beta=180 추가 스윕 불필요, beta=0 데이터를 부호반전해서 재사용 권장.

### 4) [2026-08-19 해결] "스크립트가 없다"던 문제 — 원본 컴퓨터에 있었는데 push가 안 됐던 것
`train_segment_classifier_singleprobe_beta0180_4seg.py`가 저장소에 없다고 막혔던 부분(위 3)~4)
문단, 다른 컴퓨터에서 작성) 원인 확인함: 이 스크립트는 **삭제되거나 오기(typo)인 게 아니라,
실제로 원본 컴퓨터 로컬 디스크엔 존재했는데 git에 한 번도 커밋된 적이 없었음**(`git log --all`로
확인, 이 파일을 건드린 커밋 자체가 없었음). single-probe + 4구간(4seg) + beta0180 전부
**의도적인 사양이 맞고, 오기가 아님** — 실행 로그(`run_singleprobe_beta0180_4seg.log`)에
150k 학습이 실제로 끝까지 돌았던 기록이 남아있고, 결과도 PROJECT_STATUS.md에 이미 적힌 값과
정확히 일치함: **balanced accuracy 91.0%, 연속값 s R²=0.938, Fx/Fy/F_mag R²=0.943/0.974/0.971,
L_M/phi R²=0.976/0.975** (전부 **옛 상수(BR=0.36, 논문피팅 K1/K2) 기준** — 새 MATLAB 상수로
재학습하면 이 절대 수치는 달라질 수 있음). `train_segment_classifier_multiprobe_auxreg.py`
(multi-probe+5seg, 86.0%)는 이것보다 **이전** 단계의 결과물이고, single-probe+4seg가 이걸
대체하는 **더 나중의, 더 나은** 스크립트임 — 두 스크립트는 오타 관계가 아니라 순차적 개선
관계.

**조치**: 이 스크립트 2개(`train_segment_classifier_singleprobe_4seg.py`,
`train_segment_classifier_singleprobe_beta0180_4seg.py`)와 학습된 체크포인트 2개(각각 .pth,
277KB/-라 git에 올려도 무방)를 지금 커밋해서 push함 — 앞으로는 어느 컴퓨터에서 열어도 보임.

### 5) [2026-08-19 완료] 재학습 — SOURCES 교체 + 실행까지 전부 완료
`train_segment_classifier_singleprobe_beta0180_4seg.py`의 `SOURCES`를 옛 4개 파일에서
`["fea_lm_phi_pos_matv2_all.json"]` 1개로 교체 완료(커밋됨). **부호반전 증강은 필요 없었음** —
당초 계획은 "beta=0 데이터를 부호반전해서 beta=180 대신 쓰자"였는데, 실제로 스윕해보니
beta=180 실측이 이미 48개 있었음(beta=0 133개 + beta=180 48개 = 181개, 전부 실측). 부호반전
관계를 직접 확인해보니 Fx/Fy/tip_ux/tip_uy/tip_theta는 깔끔하게 부호만 반전되는데
**tip_uz는 그렇지 않음**(예: uz0=0.102mm ↔ uz180=-0.001mm, 단순 부호반전이 아님) — 괜히
전체를 자동 부호반전으로 증강했으면 uz 쪽에 조용한 버그가 생겼을 뻔함. 실측 181개를 그대로
쓰는 지금 방식이 더 안전함.

**실행 완료(2026-08-19, CalculiX/GPU 컴퓨터에서)**: 대체모델(surrogate) 재학습 → 15만개
합성데이터 생성 → CNN 재학습(150k) 전부 완료, 총 24.3분(GPU RTX 5080). 결과는 문서 맨 위
표 참고 — balanced acc 91.0%→91.8%, s R² 0.938→0.961로 오히려 개선, 힘/형상 회귀는 소폭
하락하되 전부 R²>0.93 유지. 상수가 바뀌었으니 절대 수치가 달라지는 건 예상된 것이었고, 큰
폭 저하가 아니라 문제 없음. 모델 저장: `models/position_segment_classifier_singleprobe_beta0180_4seg.pth`
(기존 파일 덮어씀 - 필요시 git 히스토리에서 옛 버전 복구 가능).

**진행 전 참고(해결됨)**: 아래 "K1 구간 니티놀 와이어 FEA 실험"에서 와이어를 넣어도 접촉힘이
거의 안 변하는 결과가 나왔던 원인은 규명 완료(물리적으로도 맞는 결과 - 논문상 와이어는 접착
안 된 슬라이딩 텐던). **재학습을 막을 이유 없음, 바로 진행 가능.**

### 6) [신규] K1 구간 니티놀 와이어 FEA 실험 (2026-08-18, 사용자 요청)
와이어를 실제로 FEA에 넣어서 K1 구간(베이스~MOM) 접촉힘/굽힘강성 기여를 확인해봄. 사용자 제공
실측값: 지름 100um(반지름 0.05mm), E=28GPa, 포아송비 0.33(니티놀 전형값)으로 가정, 선형탄성
근사(초탄성/상변태 거동은 모델링 안 함).

**시행착오 (참고용, 재시도 불필요)**:
1. 와이어를 실제 지름(0.1mm)의 3D 고체(구멍 뚫은 파이프)로 직접 메싱 → 극단적 크기비율(와이어
   0.1mm vs 튜브 100mm) 때문에 2차 곡면요소가 자코비안 음수. 메쉬를 세밀화해도 요소수만
   폭증하고 CalculiX가 세그폴트로 죽음 → 기각.
2. 1D 빔요소(B32)+CalculiX `*EMBEDDED ELEMENT`로 전환(콘크리트 속 철근과 동일 기법, 이 정도
   길이/지름비(200:1+)에서 빔이론은 사실상 정확 - 국소 응력 디테일만 포기, 접촉힘/강성 같은
   집합값은 정확도 유지). 처음엔 빔이 embed할 고체가 필요해서 원래 비어있는 내강(중심축)을
   K1 구간만 실리콘 충전재로 채우는 방식(gmsh fragment) 시도 → 직선(phi=0)은 성공했지만,
   **굽은 형상(phi≠0)에서 충전재의 독립 스플라인이 튜브 스플라인과 미세하게 어긋나** fragment 시
   쐐기모양 틈이 생겨 자코비안 음수(왜곡도 -14.97까지) → 기각.
3. **최종 채택**: 충전재 없이, **와이어를 이미 고체인 실리콘 벽 두께 안쪽(반지름 0.75mm =
   D_IN/2~D_OUT/2 중간)에 직접 임베드** — 사용자와 논의 후 결정(실제 카테터도 이런 보강재를
   속 빈 내강 정중앙보다 벽 속에 매립하는 경우가 흔해서 물리적으로도 무리 없음). 기하가 훨씬
   단순해져서 fragment 문제 자체가 사라짐. 직선/굽은(phi=-90) 케이스 모두 검증 완료 —
   **와이어를 넣어도 기존(와이어 없는) 파이프라인과 완전히 동일한 성공/실패 패턴**(같은 케이스가
   같은 이유로 똑같이 실패/성공, 새로운 문제 없음).

**검증 결과(2026-08-19 스윕 완료 후 갱신) — 효과가 사실상 없는 것으로 나옴 (원인은 바로 아래에서 규명):**
처음 단일 케이스 검증(L_M=25, phi=0, s=15mm)은 **와이어를 아직 중심축+충전재 방식으로 넣었을
때**(위 2번, 이후 기각된 접근) 얻은 값으로 F_mag +7.7%(0.0410→0.0441mN)가 나왔었음. 그런데
**최종 채택한 벽 임베드 방식(3번)으로 같은 케이스를 다시 풀면 F_mag=0.0410mN으로 와이어 없음과
소수점까지 동일**하게 나옴. 360케이스 전체 와이어 스윕(`fea_matv2wire_all.json`, 143/360 성공)을
와이어없음 스윕과 매칭 비교해도 마찬가지: K1 구간 전체(F0>0.05mN인 22개 케이스, 노이즈 영향
작은 것만) 평균 **-0.07%, 표준편차 0.29%** — 사실상 0. 즉 **벽 임베드 위치(반지름 0.75mm)에서는
와이어가 접촉힘에 거의 기여하지 않는 것으로 일관되게 나옴.**

**[2026-08-19 원인 규명, 두 세션 합침] 물리적으로 (a)가 맞고, FEA 구현은 정확히 (b)였음 — 결과적으로 서로 보강함.**

다른 컴퓨터 세션에서 논문 Section II(설계)를 확인: "윤활을 위해 30cs 실리콘 오일을 넣는다(30 cs
of silicone oil to facilitate lubrication)"는 내용이 있음(사용자 확인). 즉 와이어(그리고 MOM
캐리지)는 튜브 벽에 접착된 보강재가 아니라 **오일이 채워진 내강 속에서 거의 마찰 없이
미끄러지는 별개 부품**임 - 텐던이 MOM 위치를 옮기려면 애초에 슬라이드가 가능해야 하니 설계
목적과도 부합함. "와이어가 벽에 완전 접착"이라고 가정하고 평행축 정리로 계산하면 오히려 그
방향으로 튜브 자체보다 **~200배** 뻣뻣해진다는 비현실적인 값이 나옴(E_nitinol=28GPa, r=50um,
오프셋 0.75mm 기준) - 두 세션이 이 수치를 독립적으로 각자 계산해서 같은 결론(접착 가정은
비현실적)에 도달함.

이 세션에서는 `*NODE PRINT, NSET=N_WIRE_BEAM`으로 `.dat`에 와이어 절점 변위를 직접 확인:
141개 절점 전부 **정확히 (0, 0, 0)** — 고정단 근처뿐 아니라 접촉점 바로 근처(s=15mm, 하중이
가장 크게 걸리는 위치) 절점까지도 전혀 안 움직임. 이건 "물리적으로 거의 저항 안 함(근사치로
작은 값)"이 아니라 **와이어가 호스트 솔리드와 수치적으로 완전히 분리(mechanically
disconnected)된 채로 풀린 것** - `*EMBEDDED ELEMENT`가 CalculiX 2.17(2020년 빌드)에서 실제로
구속을 안 걸었거나 무시된 것으로 보임(카드 자체는 `.inp`에 정상적으로 들어가 있음).

**두 발견을 합치면**: "완전 접착"은 물리적으로 틀렸고(위 200배 비현실적 계산), "완전 분리"는
FEA 구현상 우연히 걸린 결과지만 - 실제 물리(윤활, 미끄러짐 가능한 텐던)에 **결과적으로 훨씬
가까운 근사**임(접착보다는 분리가 진짜에 훨씬 가까우므로). 즉 **"접촉힘에 거의 영향 없음"이라는
최종 결론 자체는 유효**하지만, 정밀하게 하려면 "완전 분리"가 아니라 "접선방향(축방향) 미끄러짐은
자유롭지만 반경방향(압축)은 채널 벽에 막힘"이라는 **단방향 접촉**으로 모델링하는 게 더
정확함(지금 방식은 그것보다 한 단계 더 느슨한 근사). 이 정밀화는 급하지 않음 - 아래 "결론" 참고.

**결론**: `include_wire=False`(와이어 미포함) 기본 파이프라인이 원래 맞는 근사였음 — 재학습을
와이어 포함 버전으로 다시 할 필요 없음, 이미 진행 중인/향후 재학습 그대로 유효. `fea_matv2wire_all.json`은
"참고용 근사 결과"로 남겨두되(방향은 맞음), 정밀한 수치가 필요해지면 위 단방향 접촉 방식으로
재구현할 것.

**여전히 안 풀린 것**: 교수님 MATLAB의 `EN=2`(K1이 K2보다 2배 뻣뻣하다는 가정)는 와이어 보강
때문이 아닐 가능성이 높아짐(와이어가 접착 안 되니 국소 강성엔 안 보탬 - 다만 이건 국소 접촉
강성 얘기고, MOM 자체의 무게/부피나 다른 구조적 차이가 K1/K2 비율에 영향 줬을 가능성은 남아
있음) - 진짜 근거가 뭔지는 미확인. 급한 사안은 아니라 실측/교수님 문의 시 확인할 항목으로만
남겨둠.

**코드**:
- `make_bent_contact_scene.py`: `build_mesh(..., include_wire=False)` 기본값 False(명시적
  opt-in 필요 - 아래 "사고" 참고). `include_wire=True`면 K1 구간(s=0~a1, a1=L_M-H_M/2)에
  벽 안쪽 임베드 와이어 빔 커브(WIRE_BEAM 물리그룹) 추가.
- `run_contact.py`: `.inp`에 `ELSET=WIRE_BEAM`이 있을 때만(mesh 파일 내용으로 자동 감지)
  NITINOL 재질 + `*BEAM SECTION`(SECTION=CIRC) + `*EMBEDDED ELEMENT, HOST ELSET=TUBE` 추가.
- 새 스윕: `sweep_lm_phi_position_matv2_wire_worker.py` + `run_lm_phi_position_matv2_wire_sweep.sh`
  — LM(4: 0,25,50,75) × phi(9) × s(10) = 360케이스, beta=0만, `include_wire=True`.
  **결과 파일 접두사를 `fea_matv2wire_*`로 분리**(재료값-검증 스윕의 `fea_lm_phi_pos_matv2_*`
  glob과 절대 안 겹치게 - 아래 사고 참고). 재료값-검증 스윕과 동시에 돌 때 자원을 나누도록
  동시성을 낮춤(-P4, --threads 4 = 16스레드, 재료값-검증 스윕의 9×4=36스레드와 합쳐 52/64).
  ```bash
  cd 현서/scripts/contact_scenarios/fea
  bash run_lm_phi_position_matv2_wire_sweep.sh   # 결과: ../../../data/contact_scenarios/fea/fea_matv2wire_all.json
  ```

**⚠️ 사고 기록(재발 방지용)**: `include_wire` 플래그 없이 `build_mesh()` 자체를 바로 고쳤을 때,
마침 그 시점에 **이미 돌고 있던 재료값-검증 스윕**(와이어와 무관한 별개 작업)이 다음 조합을
시작하면서 의도치 않게 새 와이어 코드를 탄 적이 있음(phi=-90 조합 10개 전부 메쉬 왜곡으로
실패, 저장된 데이터 오염은 없었음 - 전부 실패 처리라 결과 JSON엔 안 들어감). **교훈**: 실행
중인 스윕이 있는 상태에서 그 스윕이 매 케이스마다 새로 import하는 모듈(`make_bent_contact_scene.py`
등)을 수정할 땐, 기본 동작이 바뀌지 않는지(또는 opt-in 플래그로 막혀 있는지) 반드시 확인할 것.

## 프로젝트 개요

MSCR-MOM(자기 연성 카테터 로봇) 논문(Park et al. 2024) 재현 + 홀센서로 "충돌(접촉) 감지"
기능 확장 연구. 울산대 김현서 학생, 전기전자공학전공.

**⚠️ 파일 위치**: 예전 경로(`scripts/...`, `data/...`, `models/...`)는 저장소 루트 밑
**`현서/` 폴더 안**에 있습니다 (예: `현서/scripts/force_model/`). 이 이동이 `git mv`로
반영되지 않아서 `git status`에 대량의 "삭제됨/추적안함" 변경사항이 있는데, 원래 있던 파일
이동분은 그대로 두고(별도 정리 필요) 이번 세션에서 새로 만든 파일들만 골라서 커밋함.

- `현서/scripts/force_model/` — 논문 재현 물리모델 (K1,K2,M1 등)
- `현서/scripts/cnn_model/` — 홀센서→자석 위치추정 CNN (기존 성과, 잘 됨)
- `현서/scripts/contact_scenarios/` — 접촉(충돌) 감지 연구
- `현서/scripts/contact_scenarios/fea/` — CalculiX+Gmsh FEA 검증 (이번 세션의 주 작업 위치)

## 지금까지 확인된 것 (요약)

1. **이진 감지(접촉 유무)**: 93.8% 정확도로 성공 (`train_binary_detection.py`)
2. **위치·힘 정밀 추정(분석모델 기반, 예전 작업)**: 단일 관측으론 실패 → 능동탐색(phi 여러
   각도)으로 개선 → 2단계 NN 방식이 가장 좋음 (이상적 상한선 R²=0.87, **이번 세션에서 이 값도
   넘어섬**, 아래 참고)
3. **면접촉(폭) 추정**: 실패로 결론남 (자석 위치에 폭 정보가 거의 안 남음)
4. **`force_model.py`의 접촉힘 계산 버그**(phi≠0일 때 불안정) → FEA로 우회
5. **[신규] 구간(segment) 인지 + 정확한 위치(s) + 힘(Fx,Fy) 추정**: 이번 세션에서 전부
   실용적인 수준까지 끌어올림 — 아래 상세

## 이번 세션(2026-08-05~11)에서 한 일 — 최종 결과

### 핵심 파이프라인
1. `sweep_lm_phi_position_worker.py` — L_M(4)×phi(11: 0,±30,±60,±90,±120,±150°)×s(10구간)
   CalculiX 스윕. 케이스별 원본 산출물 자동삭제(디스크 안 불어남). 결과:
   `fea_lm_phi_pos_sweep_all.json`
2. `train_segment_classifier_multiprobe_auxreg.py` — **최종/최고 성능 스크립트**. FEA 608개
   → 대체모델(surrogate MLP, 10-앙상블) → 15만개 합성 B-field(magpylib, 11프로브 능동탐색)
   → CNN 멀티태스크(구간분류 + 연속값 s 회귀 + Fx,Fy 회귀) 학습.

### 최종 성능 (검증 15,000개 기준)
| 항목 | 결과 |
|---|---|
| 구간(5개, 20mm씩) 분류 balanced accuracy | **86.0%** (무작위 20%) |
| 연속값 s(정확한 위치, mm) 회귀 | **R²=0.954, MAE=4.37mm** |
| Fx (보드좌표) | R²=0.90 |
| Fy (보드좌표) | R²=0.88 |
| F_mag(힘 크기, Fx·Fy로부터 유도) | R²=0.92 |

**최종 모델**: `현서/models/position_segment_classifier_multiprobe_150k_11probe_auxreg.pth`
(구간 5-class + 연속값 s(1개) + Fx,Fy(2개) 동시 예측)

### 원인 규명 과정 (20.5% 무작위 → 86.0%)
| 단계 | balanced acc | 비고 |
|---|---|---|
| 단일 관측 | 20.5% | 무작위 수준 |
| 4프로브 능동탐색 | 24.8% | |
| +push_depth 고정 | 23.5% | 효과 없음(기각) |
| +대체모델 앙상블 | 26.3% | |
| 11프로브 전체스캔 | 28.7% | |
| **+센서 노이즈 제거** | **81.6%** | **결정적 원인** |
| +힘 멀티태스크 | 83.7~84.3% | |
| **+보조회귀(연속 s 동시학습)** | **86.0%** | 최종 |
| (참고) 더 큰 네트워크로만 재학습 | 83.1% | 오히려 악화, 기각 |
| (참고) beta 고정 | 84.1%(소표본) | 효과 미미, 기각 |

**결정적 원인**: 합성 데이터 생성 시 `magpy.getB()` 결과에 "센서 노이즈 시뮬레이션"으로
전체 자기장의 5% 무작위 노이즈를 넣고 있었는데, 이게 접촉으로 인한 실제 신호보다 훨씬 커서
신호를 거의 다 덮고 있었음. **실제 하드웨어는 FFT로 노이즈를 걸러내므로, 이런 합성 데이터는
노이즈 없이 만드는 게 오히려 더 정확한 시뮬레이션**(분류기가 받는 실제 입력과 유사) —
제거하자 28.7%→81.6%로 급상승. **→ 앞으로 이런 합성 B-field 데이터를 만들 때 인위적
노이즈를 넣지 말 것.**

**F_mag 버그 수정**: 대체모델이 F_mag을 직접 예측하면 "항상 0 이상"이라는 물리적 제약이 없어
22%가 마이너스로 나왔음 → Fx,Fy만 예측하고 F_mag=sqrt(Fx²+Fy²)로 유도하는 방식으로 해결
(R²0.54→0.92).

**보조회귀가 도움된 이유**: 오답의 98.4%(→최종모델은 99.9%)가 "바로 옆 구간"과의 혼동이라,
연속값 s도 같이 예측하게 하면 경계 근처 표현력이 보강됨 — 실제로 balanced accuracy 84.3%→
86.0%로 개선.

### 검증
- **실제 FEA(대체모델 근사 없는 순수 실측)로 검증**: `backtrack_real_fea_test.py` — 11개 phi
  전부 실제 계산된 (L_M,s) 조합 3개, 3/3 정답.
- 검증셋 5,000~15,000개 전수 산점도, 카테터 곡선 위 실제vs예측 시각화 다수 (아래 참고).

### 시각화 스크립트 (전부 `현서/scripts/contact_scenarios/fea/`, 결과는 `현서/data/contact_scenarios/fea/*.png`)
- `plot_concept_diagrams.py` → `concept_5segments.png`, `concept_beta.png`, `concept_force.png`
  (5구간/beta/힘 측정 개념도)
- `plot_validation_cases_and_scatter.py` → `validation_cases_3panel.png`,
  `scatter_force.png`, `scatter_segment.png` (실제vs예측 3사례 + 산점도)
- `plot_real_deformation_3panel.py` / `plot_real_deformation_with_prediction.py` →
  `real_deformation_3panel.png`, `real_deformation_with_prediction.png` (팁변위 기반 강체회전
  재구성으로 카테터 전체 변형형상 시각화, 실제FEA vs 모델예측 겹쳐그림)
- `plot_s_regression_6panel.py` → `s_regression_6panel.png` (연속값 s 예측, 6사례, phi=0 제외
  하고 다양한 L_M/phi로 선정. 화살표는 실측 팁변위(tip_ux,tip_uy) 방향과 반드시 일치하게 그릴 것
  — 처음에 "표면 법선" 같은 별개 값으로 그렸다가 화살표·변형모양이 안 맞아서 헤맸음)

### 안 써도 되는/실패한 시도 (참고용, 재시도 불필요)
- push_depth, beta를 고정해서 테스트 → 둘 다 효과 없었음(진짜 범인은 노이즈였음)
- FEA 재시도 시 INC(허용 증분수)를 늘려서 수렴 성공률 올리기 시도 → 실패시간만 늘어나고
  성공률 개선 안 됨(기각). s가 클수록(팁에 가까울수록) FEA 수렴 실패율이 원래 높음(91%→7%,
  지렛대 효과로 팁 근처는 물리적으로 신호가 원래 약함 - 버그 아님)
- CNN을 더 크게(32/64채널)+코사인LR스케줄로 재학습 → 83.1%로 오히려 악화(과적합 추정)

### 남은 일
- git 정리(파일 이동분) 안 됨 — 필요시 `git add`로 이동 반영
- 233GB 예전 CalculiX 원본산출물 여전히 디스크에 있음(재계산 가능, `.gitignore` 제외됨)
- **하드웨어 실측 검증이 다음 단계**: 지금까지는 전부 시뮬레이션(FEA+대체모델+magpylib) 기준.
  노이즈 제거 가정("FFT로 필터링할 것")도 실제로 확인 필요.
- 대용량 합성 데이터셋(`segment_bfield*.npz`, 케이스당 최대 476MB)은 git에 안 올림
  (`.gitignore` 추가함) — 필요시 학습 스크립트 재실행으로 재생성(15만개 기준 ~110분/GPU 학습
  포함).

## 세션 간 기억(memory) 시스템

`/home/user/.claude-hyunseo/projects/-home-user-mscr-mom/memory/`에 지속 메모리 있음. 특히:
- `feedback_no_sensor_noise.md` — 합성 B-field 데이터에 노이즈 넣지 말 것
- `project_segment_classification_investigation.md` — 구간분류 실험 전체 기록

## 환경 설정

```powershell
pip install -r requirements.txt
conda install -c conda-forge calculix
```
GPU(CUDA) 있으면 최종 CNN 학습이 몇 분 내로 끝남(이번 세션 컴퓨터: 64코어+CUDA, 15만개
합성+학습 총 ~110분/회).
